/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataComponentGenerateDataComponentRenderSync } from '../../funcs/dataComponentGenerateDataComponentRenderSync.js';
import { GenerateDataComponentRenderSyncRequest$zodSchema } from '../../models/generatedatacomponentrendersyncop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: GenerateDataComponentRenderSyncRequest$zodSchema,
};

export const tool$dataComponentGenerateDataComponentRenderSync: ToolDefinition<typeof args> = {
  name: 'data-component-generate-data-component-render-sync',
  description: `Generate Data Component Render (Synchronous)

Used to generate a UI component for a given data component`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataComponentGenerateDataComponentRenderSync(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } }
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
