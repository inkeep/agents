/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { encodeSimple } from "../lib/encodings.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { pathToFunc } from "../lib/url.js";
import { DeleteDataComponentRequest$zodSchema, DeleteDataComponentResponse$zodSchema, } from "../models/deletedatacomponentop.js";
import { APIPromise } from "../types/async.js";
/**
 * Delete Data Component
 */
export function dataComponentDeleteDataComponent(client$, request, options) {
    return new APIPromise($do(client$, request, options));
}
async function $do(client$, request, options) {
    const parsed$ = safeParse(request, (value$) => DeleteDataComponentRequest$zodSchema.parse(value$), "Input validation failed");
    if (!parsed$.ok) {
        return [parsed$, { status: "invalid" }];
    }
    const payload$ = parsed$.value;
    const body$ = null;
    const pathParams$ = {
        id: encodeSimple("id", payload$.id, {
            explode: false,
            charEncoding: "percent",
        }),
        projectId: encodeSimple("projectId", payload$.projectId, {
            explode: false,
            charEncoding: "percent",
        }),
        tenantId: encodeSimple("tenantId", payload$.tenantId, {
            explode: false,
            charEncoding: "percent",
        }),
    };
    const path$ = pathToFunc("/tenants/{tenantId}/projects/{projectId}/data-components/{id}")(pathParams$);
    const headers$ = new Headers(compactMap({
        Accept: "application/json",
    }));
    const context = {
        options: client$._options,
        baseURL: options?.serverURL ?? client$._baseURL ?? "",
        operationID: "delete-data-component",
        oAuth2Scopes: null,
        resolvedSecurity: null,
        securitySource: null,
        retryConfig: options?.retries
            || client$._options.retryConfig
            || { strategy: "none" },
        retryCodes: options?.retryCodes || [
            "429",
            "500",
            "502",
            "503",
            "504",
        ],
    };
    const requestRes = client$._createRequest(context, {
        method: "DELETE",
        baseURL: options?.serverURL,
        path: path$,
        headers: headers$,
        body: body$,
        userAgent: client$._options.userAgent,
        timeoutMs: options?.timeoutMs || client$._options.timeoutMs
            || -1,
    }, options);
    if (!requestRes.ok) {
        return [requestRes, { status: "invalid" }];
    }
    const req$ = requestRes.value;
    const doResult = await client$._do(req$, {
        context,
        errorCodes: [],
        retryConfig: context.retryConfig,
        retryCodes: context.retryCodes,
    });
    if (!doResult.ok) {
        return [doResult, { status: "request-error", request: req$ }];
    }
    const response = doResult.value;
    const responseFields$ = {
        HttpMeta: { Response: response, Request: req$ },
    };
    const [result$] = await M.match(M.nil(204, DeleteDataComponentResponse$zodSchema), M.json(404, DeleteDataComponentResponse$zodSchema, {
        key: "ErrorResponse",
    }))(response, req$, { extraFields: responseFields$ });
    return [result$, { status: "complete", request: req$, response }];
}
//# sourceMappingURL=dataComponentDeleteDataComponent.js.map