/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 553a9e47a627
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export type Author = { name: string; email: string };

export const Author$zodSchema: z.ZodType<Author> = z.object({
  email: z.string(),
  name: z.string(),
});

export const RowDefaultPick = {
  Ours: "ours",
  Theirs: "theirs",
} as const;
export type RowDefaultPick = ClosedEnum<typeof RowDefaultPick>;

export const RowDefaultPick$zodSchema = z.enum([
  "ours",
  "theirs",
]);

export const Columns = {
  Ours: "ours",
  Theirs: "theirs",
} as const;
export type Columns = ClosedEnum<typeof Columns>;

export const Columns$zodSchema = z.enum([
  "ours",
  "theirs",
]);

export type Resolution = {
  table: string;
  primaryKey: { [k: string]: string };
  rowDefaultPick: RowDefaultPick;
  columns?: { [k: string]: Columns } | undefined;
};

export const Resolution$zodSchema: z.ZodType<Resolution> = z.object({
  columns: z.record(z.string(), Columns$zodSchema).optional(),
  primaryKey: z.record(z.string(), z.string()),
  rowDefaultPick: RowDefaultPick$zodSchema,
  table: z.string(),
});

export type MergeExecuteRequest = {
  sourceBranch: string;
  targetBranch: string;
  sourceHash: string;
  targetHash: string;
  message?: string | undefined;
  author?: Author | undefined;
  resolutions?: Array<Resolution> | undefined;
};

export const MergeExecuteRequest$zodSchema: z.ZodType<MergeExecuteRequest> = z
  .object({
    author: z.lazy(() => Author$zodSchema).optional(),
    message: z.string().optional(),
    resolutions: z.array(z.lazy(() => Resolution$zodSchema)).optional(),
    sourceBranch: z.string(),
    sourceHash: z.string(),
    targetBranch: z.string(),
    targetHash: z.string(),
  });
