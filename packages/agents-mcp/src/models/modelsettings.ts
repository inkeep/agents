/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 07ad8e442652
 */

import * as z from "zod";

export type ModelSettings = {
  model?: string | undefined;
  providerOptions?: { [k: string]: any | null } | undefined;
  fallbackModels?: Array<string> | undefined;
  allowedProviders?: Array<string> | undefined;
};

export const ModelSettings$zodSchema: z.ZodType<ModelSettings> = z.object({
  allowedProviders: z.array(z.string()).optional().describe(
    "Restrict and prioritize which providers can serve requests. Order determines preference. Requires AI Gateway. (e.g. [\"bedrock\", \"anthropic\"]).",
  ),
  fallbackModels: z.array(z.string()).optional().describe(
    "Ordered list of fallback models if the primary fails. Requires AI Gateway. Format: provider/model (e.g. \"openai/gpt-5.2\").",
  ),
  model: z.string().optional().describe("The model to use for the project."),
  providerOptions: z.record(z.string(), z.any().nullable()).optional().describe(
    "The provider options to use for the project.",
  ),
});
