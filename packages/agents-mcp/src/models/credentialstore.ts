/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export const CredentialStoreType = {
  Memory: "memory",
  Keychain: "keychain",
  Nango: "nango",
} as const;
export type CredentialStoreType = ClosedEnum<typeof CredentialStoreType>;

export const CredentialStoreType$zodSchema = z.enum([
  "memory",
  "keychain",
  "nango",
]);

export type CredentialStore = {
  id: string;
  type: CredentialStoreType;
  available: boolean;
  reason: string | null;
};

export const CredentialStore$zodSchema: z.ZodType<CredentialStore> = z.object({
  available: z.boolean(),
  id: z.string(),
  reason: z.string().nullable(),
  type: CredentialStoreType$zodSchema,
});
