/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 53d2e96a777f
 */

import * as z from "zod";
import { collectExtraKeys as collectExtraKeys$ } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";

export const JsonSchemaPropertySchemaTypeObject = {
  Object: "object",
} as const;
export type JsonSchemaPropertySchemaTypeObject = ClosedEnum<
  typeof JsonSchemaPropertySchemaTypeObject
>;

export const JsonSchemaPropertySchemaTypeObject$zodSchema = z.enum([
  "object",
]);

export type JsonSchemaPropertySchemaObject1 = {
  type: JsonSchemaPropertySchemaTypeObject;
  description: string;
  properties?: {
    [k: string]:
      | (JsonSchemaPropertySchema & { type: "string" })
      | (JsonSchemaPropertySchema & { type: "number" })
      | (JsonSchemaPropertySchema & { type: "integer" })
      | (JsonSchemaPropertySchema & { type: "boolean" })
      | (JsonSchemaPropertySchema & { type: "null" })
      | (JsonSchemaPropertySchemaArray1 & { type: "array" })
      | (JsonSchemaPropertySchemaObject1 & { type: "object" });
  } | undefined;
  AdditionalProperties?: { [k: string]: any | null };
};

export const JsonSchemaPropertySchemaObject1$zodSchema: z.ZodType<
  JsonSchemaPropertySchemaObject1
> = collectExtraKeys$(
  z.object({
    description: z.string(),
    properties: z.record(
      z.string(),
      z.union([
        z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
          z.object({
            type: z.literal("string"),
          }).transform((v) => ({ type: v.type })),
        ),
        z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
          z.object({
            type: z.literal("number"),
          }).transform((v) => ({ type: v.type })),
        ),
        z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
          z.object({
            type: z.literal("integer"),
          }).transform((v) => ({ type: v.type })),
        ),
        z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
          z.object({
            type: z.literal("boolean"),
          }).transform((v) => ({ type: v.type })),
        ),
        z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
          z.object({
            type: z.literal("null"),
          }).transform((v) => ({ type: v.type })),
        ),
        z.lazy(() => JsonSchemaPropertySchemaArray1$zodSchema).and(
          z.object({
            type: z.literal("array"),
          }).transform((v) => ({ type: v.type })),
        ),
        z.lazy(() => JsonSchemaPropertySchemaObject1$zodSchema).and(
          z.object({
            type: z.literal("object"),
          }).transform((v) => ({ type: v.type })),
        ),
      ]),
    ).optional(),
    type: JsonSchemaPropertySchemaTypeObject$zodSchema,
  }).catchall(z.any()),
  "AdditionalProperties",
  true,
);

export const JsonSchemaPropertySchemaObject1$zodSchemaOutbound: z.ZodType<any> =
  z.object({
    AdditionalProperties: z.record(z.string(), z.any()).optional(),
    description: z.string(),
    properties: z.record(
      z.string(),
      z.union([
        z.lazy(() => JsonSchemaPropertySchema$zodSchemaOutbound).and(
          z.object({ type: z.literal("string") }).transform((v) => ({
            type: v.type,
          })),
        ),
        z.lazy(() => JsonSchemaPropertySchema$zodSchemaOutbound).and(
          z.object({ type: z.literal("number") }).transform((v) => ({
            type: v.type,
          })),
        ),
        z.lazy(() => JsonSchemaPropertySchema$zodSchemaOutbound).and(
          z.object({ type: z.literal("integer") }).transform((v) => ({
            type: v.type,
          })),
        ),
        z.lazy(() => JsonSchemaPropertySchema$zodSchemaOutbound).and(
          z.object({ type: z.literal("boolean") }).transform((v) => ({
            type: v.type,
          })),
        ),
        z.lazy(() => JsonSchemaPropertySchema$zodSchemaOutbound).and(
          z.object({ type: z.literal("null") }).transform((v) => ({
            type: v.type,
          })),
        ),
        z.lazy(() => JsonSchemaPropertySchemaArray1$zodSchemaOutbound).and(
          z.object({ type: z.literal("array") }).transform((v) => ({
            type: v.type,
          })),
        ),
        z.lazy(() => JsonSchemaPropertySchemaObject1$zodSchemaOutbound).and(
          z.object({ type: z.literal("object") }).transform((v) => ({
            type: v.type,
          })),
        ),
      ]),
    ).optional(),
    type: JsonSchemaPropertySchemaTypeObject$zodSchema,
  }).transform((v) => {
    const { AdditionalProperties, ...rest } = v;
    return {
      ...rest,
      ...AdditionalProperties,
    };
  });

export const TypeArray = {
  Array: "array",
} as const;
export type TypeArray = ClosedEnum<typeof TypeArray>;

export const TypeArray$zodSchema = z.enum([
  "array",
]);

export type Items1 =
  | (JsonSchemaPropertySchema & { type: "string" })
  | (JsonSchemaPropertySchema & { type: "number" })
  | (JsonSchemaPropertySchema & { type: "integer" })
  | (JsonSchemaPropertySchema & { type: "boolean" })
  | (JsonSchemaPropertySchema & { type: "null" })
  | (JsonSchemaPropertySchemaArray1 & { type: "array" })
  | (JsonSchemaPropertySchemaObject1 & { type: "object" })
  | Array<JsonSchemaPropertySchemaUnion>;

export const Items1$zodSchema: z.ZodType<Items1> = z.union([
  z.union([
    z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
      z.object({
        type: z.literal("string"),
      }).transform((v) => ({ type: v.type })),
    ),
    z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
      z.object({
        type: z.literal("number"),
      }).transform((v) => ({ type: v.type })),
    ),
    z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
      z.object({
        type: z.literal("integer"),
      }).transform((v) => ({ type: v.type })),
    ),
    z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
      z.object({
        type: z.literal("boolean"),
      }).transform((v) => ({ type: v.type })),
    ),
    z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
      z.object({
        type: z.literal("null"),
      }).transform((v) => ({ type: v.type })),
    ),
    z.lazy(() => JsonSchemaPropertySchemaArray1$zodSchema).and(
      z.object({
        type: z.literal("array"),
      }).transform((v) => ({ type: v.type })),
    ),
    z.lazy(() => JsonSchemaPropertySchemaObject1$zodSchema).and(
      z.object({
        type: z.literal("object"),
      }).transform((v) => ({ type: v.type })),
    ),
  ]),
  z.array(z.lazy(() => JsonSchemaPropertySchemaUnion$zodSchema)),
]);

export type JsonSchemaPropertySchemaArray1 = {
  type: TypeArray;
  description: string;
  items?:
    | (JsonSchemaPropertySchema & { type: "string" })
    | (JsonSchemaPropertySchema & { type: "number" })
    | (JsonSchemaPropertySchema & { type: "integer" })
    | (JsonSchemaPropertySchema & { type: "boolean" })
    | (JsonSchemaPropertySchema & { type: "null" })
    | (JsonSchemaPropertySchemaArray1 & { type: "array" })
    | (JsonSchemaPropertySchemaObject1 & { type: "object" })
    | Array<JsonSchemaPropertySchemaUnion>
    | undefined;
  AdditionalProperties?: { [k: string]: any | null };
};

export const JsonSchemaPropertySchemaArray1$zodSchema: z.ZodType<
  JsonSchemaPropertySchemaArray1
> = collectExtraKeys$(
  z.object({
    description: z.string(),
    items: z.union([
      z.union([
        z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
          z.object({
            type: z.literal("string"),
          }).transform((v) => ({ type: v.type })),
        ),
        z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
          z.object({
            type: z.literal("number"),
          }).transform((v) => ({ type: v.type })),
        ),
        z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
          z.object({
            type: z.literal("integer"),
          }).transform((v) => ({ type: v.type })),
        ),
        z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
          z.object({
            type: z.literal("boolean"),
          }).transform((v) => ({ type: v.type })),
        ),
        z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
          z.object({
            type: z.literal("null"),
          }).transform((v) => ({ type: v.type })),
        ),
        z.lazy(() => JsonSchemaPropertySchemaArray1$zodSchema).and(
          z.object({
            type: z.literal("array"),
          }).transform((v) => ({ type: v.type })),
        ),
        z.lazy(() => JsonSchemaPropertySchemaObject1$zodSchema).and(
          z.object({
            type: z.literal("object"),
          }).transform((v) => ({ type: v.type })),
        ),
      ]),
      z.array(z.lazy(() => JsonSchemaPropertySchemaUnion$zodSchema)),
    ]).optional(),
    type: TypeArray$zodSchema,
  }).catchall(z.any()),
  "AdditionalProperties",
  true,
);

export const JsonSchemaPropertySchemaArray1$zodSchemaOutbound: z.ZodType<any> =
  z.object({
    AdditionalProperties: z.record(z.string(), z.any()).optional(),
    description: z.string(),
    items: z.union([
      z.union([
        z.lazy(() => JsonSchemaPropertySchema$zodSchemaOutbound).and(
          z.object({ type: z.literal("string") }).transform((v) => ({
            type: v.type,
          })),
        ),
        z.lazy(() =>
          JsonSchemaPropertySchema$zodSchemaOutbound
        ).and(
          z.object({ type: z.literal("number") }).transform((v) => ({
            type: v.type,
          })),
        ),
        z.lazy(() => JsonSchemaPropertySchema$zodSchemaOutbound).and(
          z.object({ type: z.literal("integer") }).transform((v) => ({
            type: v.type,
          })),
        ),
        z.lazy(() => JsonSchemaPropertySchema$zodSchemaOutbound).and(
          z.object({ type: z.literal("boolean") }).transform((v) => ({
            type: v.type,
          })),
        ),
        z.lazy(() => JsonSchemaPropertySchema$zodSchemaOutbound).and(
          z.object({ type: z.literal("null") }).transform((v) => ({
            type: v.type,
          })),
        ),
        z.lazy(() => JsonSchemaPropertySchemaArray1$zodSchemaOutbound).and(
          z.object({ type: z.literal("array") }).transform((v) => ({
            type: v.type,
          })),
        ),
        z.lazy(() => JsonSchemaPropertySchemaObject1$zodSchemaOutbound).and(
          z.object({ type: z.literal("object") }).transform((v) => ({
            type: v.type,
          })),
        ),
      ]),
      z.array(z.lazy(() => JsonSchemaPropertySchemaUnion$zodSchema)),
    ]).optional(),
    type: TypeArray$zodSchema,
  }).transform((v) => {
    const { AdditionalProperties, ...rest } = v;
    return {
      ...rest,
      ...AdditionalProperties,
    };
  });

export const JsonSchemaPropertySchemaType = {
  String: "string",
  Number: "number",
  Integer: "integer",
  Boolean: "boolean",
  Null: "null",
} as const;
export type JsonSchemaPropertySchemaType = ClosedEnum<
  typeof JsonSchemaPropertySchemaType
>;

export const JsonSchemaPropertySchemaType$zodSchema = z.enum([
  "string",
  "number",
  "integer",
  "boolean",
  "null",
]);

export type JsonSchemaPropertySchema = {
  type: JsonSchemaPropertySchemaType;
  description: string;
  AdditionalProperties?: { [k: string]: any | null };
};

export const JsonSchemaPropertySchema$zodSchema: z.ZodType<
  JsonSchemaPropertySchema
> = collectExtraKeys$(
  z.object({
    description: z.string(),
    type: JsonSchemaPropertySchemaType$zodSchema,
  }).catchall(z.any()),
  "AdditionalProperties",
  true,
);

export const JsonSchemaPropertySchema$zodSchemaOutbound: z.ZodType<any> = z
  .object({
    AdditionalProperties: z.record(z.string(), z.any()).optional(),
    description: z.string(),
    type: JsonSchemaPropertySchemaType$zodSchema,
  }).transform((v) => {
    const { AdditionalProperties, ...rest } = v;
    return {
      ...rest,
      ...AdditionalProperties,
    };
  });

export type JsonSchemaPropertySchemaUnion =
  | (JsonSchemaPropertySchema & { type: "string" })
  | (JsonSchemaPropertySchema & { type: "number" })
  | (JsonSchemaPropertySchema & { type: "integer" })
  | (JsonSchemaPropertySchema & { type: "boolean" })
  | (JsonSchemaPropertySchema & { type: "null" })
  | (JsonSchemaPropertySchemaArray1 & { type: "array" })
  | (JsonSchemaPropertySchemaObject1 & { type: "object" });

export const JsonSchemaPropertySchemaUnion$zodSchema: z.ZodType<
  JsonSchemaPropertySchemaUnion
> = z.union([
  z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
    z.object({
      type: z.literal("string"),
    }).transform((v) => ({ type: v.type })),
  ),
  z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
    z.object({
      type: z.literal("number"),
    }).transform((v) => ({ type: v.type })),
  ),
  z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
    z.object({
      type: z.literal("integer"),
    }).transform((v) => ({ type: v.type })),
  ),
  z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
    z.object({
      type: z.literal("boolean"),
    }).transform((v) => ({ type: v.type })),
  ),
  z.lazy(() => JsonSchemaPropertySchema$zodSchema).and(
    z.object({
      type: z.literal("null"),
    }).transform((v) => ({ type: v.type })),
  ),
  z.lazy(() => JsonSchemaPropertySchemaArray1$zodSchema).and(
    z.object({
      type: z.literal("array"),
    }).transform((v) => ({ type: v.type })),
  ),
  z.lazy(() => JsonSchemaPropertySchemaObject1$zodSchema).and(
    z.object({
      type: z.literal("object"),
    }).transform((v) => ({ type: v.type })),
  ),
]);
