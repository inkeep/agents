/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

/**
 * Location of the signature in the incoming request
 */
export const SignatureSourceSource = {
  Header: "header",
  Query: "query",
  Body: "body",
} as const;
/**
 * Location of the signature in the incoming request
 */
export type SignatureSourceSource = ClosedEnum<typeof SignatureSourceSource>;

export const SignatureSourceSource$zodSchema = z.enum([
  "header",
  "query",
  "body",
]).describe("Location of the signature in the incoming request");

/**
 * Configuration for extracting the signature
 */
export type SignatureSource = {
  source: SignatureSourceSource;
  key: string;
  prefix?: string | undefined;
  regex?: string | undefined;
};

export const SignatureSource$zodSchema: z.ZodType<SignatureSource> = z.object({
  key: z.string(),
  prefix: z.string().optional(),
  regex: z.string().optional(),
  source: SignatureSourceSource$zodSchema,
}).describe("Configuration for extracting the signature");
