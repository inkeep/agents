/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f3eaa26161dd
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

/**
 * A short code indicating the error code returned.
 */
export const UnauthorizedCode1 = {
  Unauthorized: "unauthorized",
} as const;
/**
 * A short code indicating the error code returned.
 */
export type UnauthorizedCode1 = ClosedEnum<typeof UnauthorizedCode1>;

export const UnauthorizedCode1$zodSchema = z.enum([
  "unauthorized",
]).describe("A short code indicating the error code returned.");

/**
 * A short code indicating the error code returned.
 */
export const UnauthorizedCode2 = {
  Unauthorized: "unauthorized",
} as const;
/**
 * A short code indicating the error code returned.
 */
export type UnauthorizedCode2 = ClosedEnum<typeof UnauthorizedCode2>;

export const UnauthorizedCode2$zodSchema = z.enum([
  "unauthorized",
]).describe("A short code indicating the error code returned.");

/**
 * Legacy error format for backward compatibility.
 */
export type UnauthorizedError = { code: UnauthorizedCode2; message: string };

export const UnauthorizedError$zodSchema: z.ZodType<UnauthorizedError> = z
  .object({
    code: UnauthorizedCode2$zodSchema.describe(
      "A short code indicating the error code returned.",
    ),
    message: z.string().describe(
      "A concise error message suitable for display to end users. May be truncated if the full detail is long.",
    ),
  }).describe("Legacy error format for backward compatibility.");

export type Unauthorized = {
  title: string;
  status: number;
  detail: string;
  instance?: string | undefined;
  requestId?: string | undefined;
  code: UnauthorizedCode1;
  error: UnauthorizedError;
};

export const Unauthorized$zodSchema: z.ZodType<Unauthorized> = z.object({
  code: UnauthorizedCode1$zodSchema.describe(
    "A short code indicating the error code returned.",
  ),
  detail: z.string().describe(
    "A detailed explanation specific to this occurrence of the problem, providing context and specifics about what went wrong.",
  ),
  error: z.lazy(() => UnauthorizedError$zodSchema).describe(
    "Legacy error format for backward compatibility.",
  ),
  instance: z.string().optional().describe(
    "A URI reference that identifies the specific occurrence of the problem.",
  ),
  requestId: z.string().optional().describe(
    "A unique identifier for the request, useful for troubleshooting.",
  ),
  status: z.int().describe("The HTTP status code."),
  title: z.string().describe(
    "A short, human-readable summary of the problem type.",
  ),
});
