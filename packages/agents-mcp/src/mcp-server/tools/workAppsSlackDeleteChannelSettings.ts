/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 33c5fc3dc8f3
 */

import { workAppsSlackDeleteChannelSettings } from "../../funcs/workAppsSlackDeleteChannelSettings.js";
import { SlackDeleteChannelSettingsRequest$zodSchema } from "../../models/slackdeletechannelsettingsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SlackDeleteChannelSettingsRequest$zodSchema,
};

export const tool$workAppsSlackDeleteChannelSettings: ToolDefinition<
  typeof args
> = {
  name: "work-apps-slack-delete-channel-settings",
  description: `Remove Channel Config

Remove the default agent configuration for a channel`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workAppsSlackDeleteChannelSettings(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
