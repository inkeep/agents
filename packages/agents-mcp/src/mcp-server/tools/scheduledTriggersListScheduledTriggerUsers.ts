/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 2a4cb46382f5
 */

import { scheduledTriggersListScheduledTriggerUsers } from "../../funcs/scheduledTriggersListScheduledTriggerUsers.js";
import { ListScheduledTriggerUsersRequest$zodSchema } from "../../models/listscheduledtriggerusersop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListScheduledTriggerUsersRequest$zodSchema,
};

export const tool$scheduledTriggersListScheduledTriggerUsers: ToolDefinition<
  typeof args
> = {
  name: "scheduled-triggers-list-scheduled-trigger-users",
  description: `List Scheduled Trigger Users`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await scheduledTriggersListScheduledTriggerUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
