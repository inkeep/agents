/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 9bb59635682d
 */

import { workflowsGetApiCronCleanupStreamChunks } from "../../funcs/workflowsGetApiCronCleanupStreamChunks.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$workflowsGetApiCronCleanupStreamChunks: ToolDefinition = {
  name: "workflows-get-api-cron-cleanup-stream-chunks",
  description: `Clean up expired stream chunks

Called by Vercel Cron to delete stream chunks older than 5 minutes. Auth via CRON_SECRET or INKEEP_AGENTS_RUN_API_BYPASS_SECRET.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await workflowsGetApiCronCleanupStreamChunks(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
