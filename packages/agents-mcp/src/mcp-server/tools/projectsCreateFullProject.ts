/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ef0cd8c061a4
 */

import { projectsCreateFullProject } from "../../funcs/projectsCreateFullProject.js";
import { CreateFullProjectRequest$zodSchema } from "../../models/createfullprojectop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateFullProjectRequest$zodSchema,
};

export const tool$projectsCreateFullProject: ToolDefinition<typeof args> = {
  name: "projects-create-full-project",
  description: `Create Full Project

Create a complete project with all Agents, Sub Agents, tools, and relationships from JSON definition`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectsCreateFullProject(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
