/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 22d7d8ba8dd5
 */

import { workflowsPostApiDeployRestartScheduler } from "../../funcs/workflowsPostApiDeployRestartScheduler.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$workflowsPostApiDeployRestartScheduler: ToolDefinition = {
  name: "workflows-post-api-deploy-restart-scheduler",
  description: `Restart scheduler workflow on new deployment

Called by CI after deploy to restart the scheduler workflow on the latest deployment. Auth via INKEEP_AGENTS_RUN_API_BYPASS_SECRET.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  tool: async (client, ctx) => {
    const [result] = await workflowsPostApiDeployRestartScheduler(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
