/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a6431ef7554d
 */

import { appsDeleteApp } from "../../funcs/appsDeleteApp.js";
import { DeleteAppRequest$zodSchema } from "../../models/deleteappop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteAppRequest$zodSchema,
};

export const tool$appsDeleteApp: ToolDefinition<typeof args> = {
  name: "apps-delete-app",
  description: `Delete App

Delete an app credential permanently`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await appsDeleteApp(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
