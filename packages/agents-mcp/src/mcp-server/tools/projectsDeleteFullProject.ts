/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 340133bf7397
 */

import { projectsDeleteFullProject } from "../../funcs/projectsDeleteFullProject.js";
import { DeleteFullProjectRequest$zodSchema } from "../../models/deletefullprojectop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteFullProjectRequest$zodSchema,
};

export const tool$projectsDeleteFullProject: ToolDefinition<typeof args> = {
  name: "projects-delete-full-project",
  description: `Delete Full Project

Delete a complete project and cascade to all related entities (Agents, Sub Agents, tools, relationships)`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectsDeleteFullProject(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
