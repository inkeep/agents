/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsEvaluateConversation } from "../../funcs/evaluationsEvaluateConversation.js";
import { EvaluateConversationRequest$zodSchema } from "../../models/evaluateconversationop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EvaluateConversationRequest$zodSchema,
};

export const tool$evaluationsEvaluateConversation: ToolDefinition<typeof args> =
  {
    name: "evaluations-evaluate-conversation",
    description: `Trigger evaluation on a single conversation`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await evaluationsEvaluateConversation(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
