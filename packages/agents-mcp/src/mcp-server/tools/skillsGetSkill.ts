/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: abef685255c4
 */

import { skillsGetSkill } from "../../funcs/skillsGetSkill.js";
import { GetSkillRequest$zodSchema } from "../../models/getskillop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetSkillRequest$zodSchema,
};

export const tool$skillsGetSkill: ToolDefinition<typeof args> = {
  name: "skills-get-skill",
  description: `Get Skill`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await skillsGetSkill(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
