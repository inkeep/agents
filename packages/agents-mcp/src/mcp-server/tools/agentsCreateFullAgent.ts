/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 64ce01c77820
 */

import { agentsCreateFullAgent } from "../../funcs/agentsCreateFullAgent.js";
import { CreateFullAgentRequest$zodSchema } from "../../models/createfullagentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateFullAgentRequest$zodSchema,
};

export const tool$agentsCreateFullAgent: ToolDefinition<typeof args> = {
  name: "agents-create-full-agent",
  description: `Create Full Agent

Create a complete agent with all agents, tools, and relationships from JSON definition`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await agentsCreateFullAgent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
