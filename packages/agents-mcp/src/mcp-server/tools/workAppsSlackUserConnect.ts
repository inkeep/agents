/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: bab0638c2314
 */

import { workAppsSlackUserConnect } from "../../funcs/workAppsSlackUserConnect.js";
import { SlackUserConnectRequest$zodSchema } from "../../models/slackuserconnectop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SlackUserConnectRequest$zodSchema.optional(),
};

export const tool$workAppsSlackUserConnect: ToolDefinition<typeof args> = {
  name: "work-apps-slack-user-connect",
  description: `Create Nango Connect Session

Create a Nango session for Slack OAuth flow. Used by the dashboard.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workAppsSlackUserConnect(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
