/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 8f8ec1ea8b3d
 */

import { skillsListSkills } from "../../funcs/skillsListSkills.js";
import { ListSkillsRequest$zodSchema } from "../../models/listskillsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListSkillsRequest$zodSchema,
};

export const tool$skillsListSkills: ToolDefinition<typeof args> = {
  name: "skills-list-skills",
  description: `List Skills`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await skillsListSkills(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
