/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: cc53f3db8ce1
 */

import { workAppsSlackGetChannelSettings } from "../../funcs/workAppsSlackGetChannelSettings.js";
import { SlackGetChannelSettingsRequest$zodSchema } from "../../models/slackgetchannelsettingsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SlackGetChannelSettingsRequest$zodSchema,
};

export const tool$workAppsSlackGetChannelSettings: ToolDefinition<typeof args> =
  {
    name: "work-apps-slack-get-channel-settings",
    description: `Get Channel Settings

Get default agent configuration for a specific channel`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await workAppsSlackGetChannelSettings(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
