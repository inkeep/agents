/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsRemoveEvaluatorFromJobConfig } from "../../funcs/evaluationsRemoveEvaluatorFromJobConfig.js";
import { RemoveEvaluatorFromJobConfigRequest$zodSchema } from "../../models/removeevaluatorfromjobconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RemoveEvaluatorFromJobConfigRequest$zodSchema,
};

export const tool$evaluationsRemoveEvaluatorFromJobConfig: ToolDefinition<
  typeof args
> = {
  name: "evaluations-remove-evaluator-from-job-config",
  description: `Remove Evaluator from Evaluation Job Config`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsRemoveEvaluatorFromJobConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
