/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 68565776f227
 */

import { feedbackCreateFeedback } from "../../funcs/feedbackCreateFeedback.js";
import { CreateFeedbackRequest$zodSchema } from "../../models/createfeedbackop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateFeedbackRequest$zodSchema,
};

export const tool$feedbackCreateFeedback: ToolDefinition<typeof args> = {
  name: "feedback-create-feedback",
  description: `Create Feedback

Create a new feedback entry for a conversation or message`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await feedbackCreateFeedback(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
