/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 3377e752bcec
 */

import { subAgentsUpdateSubAgentRelation } from "../../funcs/subAgentsUpdateSubAgentRelation.js";
import { UpdateSubAgentRelationRequest$zodSchema } from "../../models/updatesubagentrelationop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateSubAgentRelationRequest$zodSchema,
};

export const tool$subAgentsUpdateSubAgentRelation: ToolDefinition<typeof args> =
  {
    name: "sub-agents-update-sub-agent-relation",
    description: `Update Sub Agent Relation`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await subAgentsUpdateSubAgentRelation(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
