/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { triggersGetTriggerById } from "../../funcs/triggersGetTriggerById.js";
import { GetTriggerByIdRequest$zodSchema } from "../../models/gettriggerbyidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetTriggerByIdRequest$zodSchema,
};

export const tool$triggersGetTriggerById: ToolDefinition<typeof args> = {
  name: "triggers-get-trigger-by-id",
  description: `Get Trigger`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await triggersGetTriggerById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
