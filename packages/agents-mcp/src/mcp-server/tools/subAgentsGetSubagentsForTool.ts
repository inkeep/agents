/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ca12eecb512a
 */

import { subAgentsGetSubagentsForTool } from "../../funcs/subAgentsGetSubagentsForTool.js";
import { GetSubagentsForToolRequest$zodSchema } from "../../models/getsubagentsfortoolop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetSubagentsForToolRequest$zodSchema,
};

export const tool$subAgentsGetSubagentsForTool: ToolDefinition<typeof args> = {
  name: "sub-agents-get-subagents-for-tool",
  description: `Get SubAgents for Tool`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await subAgentsGetSubagentsForTool(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
