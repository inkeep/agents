/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 5068f6e54c2c
 */

import { scheduledTriggersListUpcomingScheduledRuns } from "../../funcs/scheduledTriggersListUpcomingScheduledRuns.js";
import { ListUpcomingScheduledRunsRequest$zodSchema } from "../../models/listupcomingscheduledrunsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListUpcomingScheduledRunsRequest$zodSchema,
};

export const tool$scheduledTriggersListUpcomingScheduledRuns: ToolDefinition<
  typeof args
> = {
  name: "scheduled-triggers-list-upcoming-scheduled-runs",
  description: `List Upcoming Runs`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scheduledTriggersListUpcomingScheduledRuns(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
