/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: aba7ed6076c9
 */

import { contextConfigsUpdateContextConfig } from "../../funcs/contextConfigsUpdateContextConfig.js";
import { UpdateContextConfigRequest$zodSchema } from "../../models/updatecontextconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateContextConfigRequest$zodSchema,
};

export const tool$contextConfigsUpdateContextConfig: ToolDefinition<
  typeof args
> = {
  name: "context-configs-update-context-config",
  description: `Update Context Configuration`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contextConfigsUpdateContextConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
