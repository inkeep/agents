/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 380267350df8
 */

import { skillsUpdateSkillFile } from "../../funcs/skillsUpdateSkillFile.js";
import { UpdateSkillFileRequest$zodSchema } from "../../models/updateskillfileop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateSkillFileRequest$zodSchema,
};

export const tool$skillsUpdateSkillFile: ToolDefinition<typeof args> = {
  name: "skills-update-skill-file",
  description: `Update Skill File`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await skillsUpdateSkillFile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
