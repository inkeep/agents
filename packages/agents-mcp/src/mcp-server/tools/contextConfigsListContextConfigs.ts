/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 5df93b12fb2c
 */

import { contextConfigsListContextConfigs } from "../../funcs/contextConfigsListContextConfigs.js";
import { ListContextConfigsRequest$zodSchema } from "../../models/listcontextconfigsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListContextConfigsRequest$zodSchema,
};

export const tool$contextConfigsListContextConfigs: ToolDefinition<
  typeof args
> = {
  name: "context-configs-list-context-configs",
  description: `List Context Configurations`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contextConfigsListContextConfigs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
