/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ace46c2f82ad
 */

import { toolsSetMcpToolSlackAccess } from "../../funcs/toolsSetMcpToolSlackAccess.js";
import { SetMcpToolSlackAccessRequest$zodSchema } from "../../models/setmcptoolslackaccessop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SetMcpToolSlackAccessRequest$zodSchema,
};

export const tool$toolsSetMcpToolSlackAccess: ToolDefinition<typeof args> = {
  name: "tools-set-mcp-tool-slack-access",
  description: `Set MCP tool Slack channel access

Configures which Slack channels an MCP tool can post to. When channelAccessMode is "all", the tool can post to any channel. When channelAccessMode is "selected", the tool is scoped to specific channels (channelIds required).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await toolsSetMcpToolSlackAccess(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
