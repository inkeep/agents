/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f398c661276b
 */

import { gitHubListGithubInstallations } from "../../funcs/gitHubListGithubInstallations.js";
import { ListGithubInstallationsRequest$zodSchema } from "../../models/listgithubinstallationsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListGithubInstallationsRequest$zodSchema,
};

export const tool$gitHubListGithubInstallations: ToolDefinition<typeof args> = {
  name: "git-hub-list-github-installations",
  description: `List GitHub App installations

Returns a list of GitHub App installations connected to this tenant. By default, deleted installations are filtered out. Use the includeDisconnected query parameter to include them.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await gitHubListGithubInstallations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
