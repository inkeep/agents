/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 4d56cabe45c2
 */

import { gitHubGetGithubInstallationDetails } from "../../funcs/gitHubGetGithubInstallationDetails.js";
import { GetGithubInstallationDetailsRequest$zodSchema } from "../../models/getgithubinstallationdetailsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetGithubInstallationDetailsRequest$zodSchema,
};

export const tool$gitHubGetGithubInstallationDetails: ToolDefinition<
  typeof args
> = {
  name: "git-hub-get-github-installation-details",
  description: `Get GitHub App installation details

Returns detailed information about a specific GitHub App installation, including the full list of repositories.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await gitHubGetGithubInstallationDetails(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
