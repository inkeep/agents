/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0b40e311ec0e
 */

import { skillsCreateSkill } from "../../funcs/skillsCreateSkill.js";
import { CreateSkillRequest$zodSchema } from "../../models/createskillop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateSkillRequest$zodSchema,
};

export const tool$skillsCreateSkill: ToolDefinition<typeof args> = {
  name: "skills-create-skill",
  description: `Create Skill`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await skillsCreateSkill(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
