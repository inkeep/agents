/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 9211f50cc31f
 */

import { evaluationsAddAgentToDataset } from "../../funcs/evaluationsAddAgentToDataset.js";
import { AddAgentToDatasetRequest$zodSchema } from "../../models/addagenttodatasetop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AddAgentToDatasetRequest$zodSchema,
};

export const tool$evaluationsAddAgentToDataset: ToolDefinition<typeof args> = {
  name: "evaluations-add-agent-to-dataset",
  description: `Add Agent to Dataset`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await evaluationsAddAgentToDataset(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
