/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 9584bab08d5c
 */

import { evaluationsRemoveAgentFromEvaluator } from "../../funcs/evaluationsRemoveAgentFromEvaluator.js";
import { RemoveAgentFromEvaluatorRequest$zodSchema } from "../../models/removeagentfromevaluatorop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RemoveAgentFromEvaluatorRequest$zodSchema,
};

export const tool$evaluationsRemoveAgentFromEvaluator: ToolDefinition<
  typeof args
> = {
  name: "evaluations-remove-agent-from-evaluator",
  description: `Remove Agent from Evaluator`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await evaluationsRemoveAgentFromEvaluator(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
