/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 9b069c42ed80
 */

import { gitHubDeleteGithubInstallation } from "../../funcs/gitHubDeleteGithubInstallation.js";
import { DeleteGithubInstallationRequest$zodSchema } from "../../models/deletegithubinstallationop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteGithubInstallationRequest$zodSchema,
};

export const tool$gitHubDeleteGithubInstallation: ToolDefinition<typeof args> =
  {
    name: "git-hub-delete-github-installation",
    description: `Delete a GitHub App installation permanently

Permanently deletes a GitHub App installation from the tenant. This hard deletes the installation record, all associated repositories, and project repository access entries. This action cannot be undone. Use POST /disconnect for soft delete instead. Note: This does NOT uninstall the GitHub App from GitHub - the user can do that separately from GitHub settings.`,
    annotations: {
      "title": "",
      "destructiveHint": true,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await gitHubDeleteGithubInstallation(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
