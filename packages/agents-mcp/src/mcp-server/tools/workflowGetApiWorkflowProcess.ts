/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workflowGetApiWorkflowProcess } from "../../funcs/workflowGetApiWorkflowProcess.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$workflowGetApiWorkflowProcess: ToolDefinition = {
  name: "workflow-get-api-workflow-process",
  description: `Process workflow jobs

Keeps the workflow worker active to process queued jobs (called by cron)`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await workflowGetApiWorkflowProcess(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
