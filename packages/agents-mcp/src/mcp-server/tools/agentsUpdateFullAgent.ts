/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 96d3a14009c2
 */

import { agentsUpdateFullAgent } from "../../funcs/agentsUpdateFullAgent.js";
import { UpdateFullAgentRequest$zodSchema } from "../../models/updatefullagentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateFullAgentRequest$zodSchema,
};

export const tool$agentsUpdateFullAgent: ToolDefinition<typeof args> = {
  name: "agents-update-full-agent",
  description: `Update Full Agent

Update or create a complete agent with all agents, tools, and relationships from JSON definition`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await agentsUpdateFullAgent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
