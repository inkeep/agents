/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: bde567f8f9d3
 */

import { projectPermissionsGetProjectPermissions } from "../../funcs/projectPermissionsGetProjectPermissions.js";
import { GetProjectPermissionsRequest$zodSchema } from "../../models/getprojectpermissionsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetProjectPermissionsRequest$zodSchema,
};

export const tool$projectPermissionsGetProjectPermissions: ToolDefinition<
  typeof args
> = {
  name: "project-permissions-get-project-permissions",
  description: `Get Project Permissions

Get the current user's permissions for a project. Returns which actions the user can perform.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectPermissionsGetProjectPermissions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
