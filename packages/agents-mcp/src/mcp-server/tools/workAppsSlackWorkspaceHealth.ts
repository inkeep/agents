/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ff8b0e4ee970
 */

import { workAppsSlackWorkspaceHealth } from "../../funcs/workAppsSlackWorkspaceHealth.js";
import { SlackWorkspaceHealthRequest$zodSchema } from "../../models/slackworkspacehealthop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SlackWorkspaceHealthRequest$zodSchema,
};

export const tool$workAppsSlackWorkspaceHealth: ToolDefinition<typeof args> = {
  name: "work-apps-slack-workspace-health",
  description: `Check Workspace Health

Verify the bot token is valid and check permissions. Returns bot info and permission status.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workAppsSlackWorkspaceHealth(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
