/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 3cc7b557f4a0
 */

import { workAppsSlackUserStatus } from "../../funcs/workAppsSlackUserStatus.js";
import { SlackUserStatusRequest$zodSchema } from "../../models/slackuserstatusop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SlackUserStatusRequest$zodSchema,
};

export const tool$workAppsSlackUserStatus: ToolDefinition<typeof args> = {
  name: "work-apps-slack-user-status",
  description: `Get Connection Status

Check if an Inkeep user has a linked Slack account.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workAppsSlackUserStatus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
