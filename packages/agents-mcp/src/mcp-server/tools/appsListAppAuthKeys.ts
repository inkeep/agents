/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 2cbe5b6892e5
 */

import { appsListAppAuthKeys } from "../../funcs/appsListAppAuthKeys.js";
import { ListAppAuthKeysRequest$zodSchema } from "../../models/listappauthkeysop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListAppAuthKeysRequest$zodSchema,
};

export const tool$appsListAppAuthKeys: ToolDefinition<typeof args> = {
  name: "apps-list-app-auth-keys",
  description: `List Public Keys

List all public keys configured for app authentication`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await appsListAppAuthKeys(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
