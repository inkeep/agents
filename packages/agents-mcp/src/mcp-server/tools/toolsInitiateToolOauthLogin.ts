/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 4c8cc4f9bcee
 */

import { toolsInitiateToolOauthLogin } from "../../funcs/toolsInitiateToolOauthLogin.js";
import { InitiateToolOauthLoginRequest$zodSchema } from "../../models/initiatetooloauthloginop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: InitiateToolOauthLoginRequest$zodSchema,
};

export const tool$toolsInitiateToolOauthLogin: ToolDefinition<typeof args> = {
  name: "tools-initiate-tool-oauth-login",
  description: `Initiate OAuth login for MCP tool

Detects OAuth requirements and redirects to the authorization server for the specified tool`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsInitiateToolOauthLogin(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
