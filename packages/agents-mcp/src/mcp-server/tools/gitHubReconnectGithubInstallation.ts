/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: dbb726e25ee5
 */

import { gitHubReconnectGithubInstallation } from "../../funcs/gitHubReconnectGithubInstallation.js";
import { ReconnectGithubInstallationRequest$zodSchema } from "../../models/reconnectgithubinstallationop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ReconnectGithubInstallationRequest$zodSchema,
};

export const tool$gitHubReconnectGithubInstallation: ToolDefinition<
  typeof args
> = {
  name: "git-hub-reconnect-github-installation",
  description: `Reconnect a disconnected GitHub App installation

Reconnects a previously disconnected GitHub App installation by setting its status back to "active" and syncing the available repositories from GitHub. This can only be used on installations with "disconnected" status.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await gitHubReconnectGithubInstallation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
