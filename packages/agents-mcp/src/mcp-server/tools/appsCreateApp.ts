/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: c49f321b4200
 */

import { appsCreateApp } from "../../funcs/appsCreateApp.js";
import { CreateAppRequest$zodSchema } from "../../models/createappop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateAppRequest$zodSchema,
};

export const tool$appsCreateApp: ToolDefinition<typeof args> = {
  name: "apps-create-app",
  description: `Create App

Create a new app credential.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await appsCreateApp(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
