/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 4cf427e52787
 */

import { feedbackDeleteFeedback } from "../../funcs/feedbackDeleteFeedback.js";
import { DeleteFeedbackRequest$zodSchema } from "../../models/deletefeedbackop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteFeedbackRequest$zodSchema,
};

export const tool$feedbackDeleteFeedback: ToolDefinition<typeof args> = {
  name: "feedback-delete-feedback",
  description: `Delete Feedback

Delete a feedback entry`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await feedbackDeleteFeedback(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
