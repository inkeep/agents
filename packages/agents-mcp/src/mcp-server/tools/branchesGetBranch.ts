/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: bedbd8a3d0be
 */

import { branchesGetBranch } from "../../funcs/branchesGetBranch.js";
import { GetBranchRequest$zodSchema } from "../../models/getbranchop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetBranchRequest$zodSchema,
};

export const tool$branchesGetBranch: ToolDefinition<typeof args> = {
  name: "branches-get-branch",
  description: `Get Branch

Get a single branch by name`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await branchesGetBranch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
