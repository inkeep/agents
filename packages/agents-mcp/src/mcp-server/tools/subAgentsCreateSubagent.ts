/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 90f1c9aa2ff6
 */

import { subAgentsCreateSubagent } from "../../funcs/subAgentsCreateSubagent.js";
import { CreateSubagentRequest$zodSchema } from "../../models/createsubagentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateSubagentRequest$zodSchema,
};

export const tool$subAgentsCreateSubagent: ToolDefinition<typeof args> = {
  name: "sub-agents-create-subagent",
  description: `Create SubAgent`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await subAgentsCreateSubagent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
