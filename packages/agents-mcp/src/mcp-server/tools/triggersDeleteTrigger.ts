/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { triggersDeleteTrigger } from "../../funcs/triggersDeleteTrigger.js";
import { DeleteTriggerRequest$zodSchema } from "../../models/deletetriggerop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteTriggerRequest$zodSchema,
};

export const tool$triggersDeleteTrigger: ToolDefinition<typeof args> = {
  name: "triggers-delete-trigger",
  description: `Delete Trigger`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await triggersDeleteTrigger(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
