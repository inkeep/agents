/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invitationsGetManageApiInvitationsPending } from "../../funcs/invitationsGetManageApiInvitationsPending.js";
import { GetManageApiInvitationsPendingRequest$zodSchema } from "../../models/getmanageapiinvitationspendingop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetManageApiInvitationsPendingRequest$zodSchema,
};

export const tool$invitationsGetManageApiInvitationsPending: ToolDefinition<
  typeof args
> = {
  name: "invitations-get-manage-api-invitations-pending",
  description: `Get pending invitations

Get all pending (non-expired) invitations for a given email address`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await invitationsGetManageApiInvitationsPending(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
