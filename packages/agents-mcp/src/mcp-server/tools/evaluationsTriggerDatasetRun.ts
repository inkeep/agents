/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: fa65fbaeee63
 */

import { evaluationsTriggerDatasetRun } from "../../funcs/evaluationsTriggerDatasetRun.js";
import { TriggerDatasetRunRequest$zodSchema } from "../../models/triggerdatasetrunop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TriggerDatasetRunRequest$zodSchema,
};

export const tool$evaluationsTriggerDatasetRun: ToolDefinition<typeof args> = {
  name: "evaluations-trigger-dataset-run",
  description: `Trigger Dataset Run`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await evaluationsTriggerDatasetRun(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
