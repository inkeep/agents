/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0397d0ed54fa
 */

import { projectsGetProjectGithubAccess } from "../../funcs/projectsGetProjectGithubAccess.js";
import { GetProjectGithubAccessRequest$zodSchema } from "../../models/getprojectgithubaccessop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetProjectGithubAccessRequest$zodSchema,
};

export const tool$projectsGetProjectGithubAccess: ToolDefinition<typeof args> =
  {
    name: "projects-get-project-github-access",
    description: `Get project GitHub repository access

Returns the current GitHub repository access configuration for a project. If mode is "all", the project has access to all repositories from tenant GitHub installations. If mode is "selected", the project is scoped to specific repositories.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await projectsGetProjectGithubAccess(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
