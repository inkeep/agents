/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { triggersUpdateTrigger } from "../../funcs/triggersUpdateTrigger.js";
import { UpdateTriggerRequest$zodSchema } from "../../models/updatetriggerop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateTriggerRequest$zodSchema,
};

export const tool$triggersUpdateTrigger: ToolDefinition<typeof args> = {
  name: "triggers-update-trigger",
  description: `Update Trigger`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await triggersUpdateTrigger(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
