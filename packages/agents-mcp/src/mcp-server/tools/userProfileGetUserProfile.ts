/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: fc729ed58b53
 */

import { userProfileGetUserProfile } from "../../funcs/userProfileGetUserProfile.js";
import { GetUserProfileRequest$zodSchema } from "../../models/getuserprofileop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetUserProfileRequest$zodSchema,
};

export const tool$userProfileGetUserProfile: ToolDefinition<typeof args> = {
  name: "user-profile-get-user-profile",
  description: `Get User Profile

Get the profile for a specific user. Users can only access their own profile.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await userProfileGetUserProfile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
