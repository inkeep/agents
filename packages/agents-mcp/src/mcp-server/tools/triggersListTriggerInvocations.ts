/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { triggersListTriggerInvocations } from "../../funcs/triggersListTriggerInvocations.js";
import { ListTriggerInvocationsRequest$zodSchema } from "../../models/listtriggerinvocationsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListTriggerInvocationsRequest$zodSchema,
};

export const tool$triggersListTriggerInvocations: ToolDefinition<typeof args> =
  {
    name: "triggers-list-trigger-invocations",
    description: `List Trigger Invocations`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await triggersListTriggerInvocations(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
