/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 1094f1c4891f
 */

import { scheduledTriggersListScheduledTriggers } from "../../funcs/scheduledTriggersListScheduledTriggers.js";
import { ListScheduledTriggersRequest$zodSchema } from "../../models/listscheduledtriggersop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListScheduledTriggersRequest$zodSchema,
};

export const tool$scheduledTriggersListScheduledTriggers: ToolDefinition<
  typeof args
> = {
  name: "scheduled-triggers-list-scheduled-triggers",
  description: `List Scheduled Triggers`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scheduledTriggersListScheduledTriggers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
