/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: c816148e14cf
 */

import { gitHubGetGithubInstallUrl } from "../../funcs/gitHubGetGithubInstallUrl.js";
import { GetGithubInstallUrlRequest$zodSchema } from "../../models/getgithubinstallurlop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetGithubInstallUrlRequest$zodSchema,
};

export const tool$gitHubGetGithubInstallUrl: ToolDefinition<typeof args> = {
  name: "git-hub-get-github-install-url",
  description: `Get GitHub App installation URL

Generates a URL to install the GitHub App on an organization or user account. The URL includes a signed state parameter that encodes the tenant ID and expires after 10 minutes. After installation, GitHub will redirect back to our callback endpoint with this state.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await gitHubGetGithubInstallUrl(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
