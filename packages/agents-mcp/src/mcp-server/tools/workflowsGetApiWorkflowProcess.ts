/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0fa1c42a34dd
 */

import { workflowsGetApiWorkflowProcess } from "../../funcs/workflowsGetApiWorkflowProcess.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$workflowsGetApiWorkflowProcess: ToolDefinition = {
  name: "workflows-get-api-workflow-process",
  description: `Process workflow jobs

Keeps the workflow worker active to process queued jobs (called by cron)`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await workflowsGetApiWorkflowProcess(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
