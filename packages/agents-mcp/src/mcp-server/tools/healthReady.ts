/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 74a18de29ff5
 */

import { healthReady } from "../../funcs/healthReady.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$healthReady: ToolDefinition = {
  name: "health-ready",
  description: `Readiness check

Check if the service is ready to serve traffic by verifying database connectivity`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await healthReady(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
