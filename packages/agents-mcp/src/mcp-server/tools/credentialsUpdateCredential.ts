/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0de143311e4d
 */

import { credentialsUpdateCredential } from "../../funcs/credentialsUpdateCredential.js";
import { UpdateCredentialRequest$zodSchema } from "../../models/updatecredentialop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateCredentialRequest$zodSchema,
};

export const tool$credentialsUpdateCredential: ToolDefinition<typeof args> = {
  name: "credentials-update-credential",
  description: `Update Credential`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await credentialsUpdateCredential(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
