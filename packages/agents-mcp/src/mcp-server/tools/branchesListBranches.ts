/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 886e42cc0327
 */

import { branchesListBranches } from "../../funcs/branchesListBranches.js";
import { ListBranchesRequest$zodSchema } from "../../models/listbranchesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListBranchesRequest$zodSchema,
};

export const tool$branchesListBranches: ToolDefinition<typeof args> = {
  name: "branches-list-branches",
  description: `List Branches

List all branches within a project`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await branchesListBranches(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
