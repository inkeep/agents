/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsGetEvaluatorsBatch } from "../../funcs/evaluationsGetEvaluatorsBatch.js";
import { GetEvaluatorsBatchRequest$zodSchema } from "../../models/getevaluatorsbatchop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetEvaluatorsBatchRequest$zodSchema,
};

export const tool$evaluationsGetEvaluatorsBatch: ToolDefinition<typeof args> = {
  name: "evaluations-get-evaluators-batch",
  description: `Get Evaluators by IDs`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsGetEvaluatorsBatch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
