/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: e97eaf5c4af6
 */

import { skillsCreateSkillFile } from "../../funcs/skillsCreateSkillFile.js";
import { CreateSkillFileRequest$zodSchema } from "../../models/createskillfileop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateSkillFileRequest$zodSchema,
};

export const tool$skillsCreateSkillFile: ToolDefinition<typeof args> = {
  name: "skills-create-skill-file",
  description: `Create Skill File`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await skillsCreateSkillFile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
