/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 02e146c1a4a0
 */

import { subAgentsListSubagents } from "../../funcs/subAgentsListSubagents.js";
import { ListSubagentsRequest$zodSchema } from "../../models/listsubagentsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListSubagentsRequest$zodSchema,
};

export const tool$subAgentsListSubagents: ToolDefinition<typeof args> = {
  name: "sub-agents-list-subagents",
  description: `List SubAgents`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await subAgentsListSubagents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
