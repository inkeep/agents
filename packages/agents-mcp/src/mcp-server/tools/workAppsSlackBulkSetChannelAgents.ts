/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 11c7a1ad3994
 */

import { workAppsSlackBulkSetChannelAgents } from "../../funcs/workAppsSlackBulkSetChannelAgents.js";
import { SlackBulkSetChannelAgentsRequest$zodSchema } from "../../models/slackbulksetchannelagentsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SlackBulkSetChannelAgentsRequest$zodSchema,
};

export const tool$workAppsSlackBulkSetChannelAgents: ToolDefinition<
  typeof args
> = {
  name: "work-apps-slack-bulk-set-channel-agents",
  description: `Bulk Set Channel Agents

Apply the same agent configuration to multiple channels at once`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workAppsSlackBulkSetChannelAgents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
