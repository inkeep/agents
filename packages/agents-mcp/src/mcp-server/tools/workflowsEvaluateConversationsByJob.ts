/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workflowsEvaluateConversationsByJob } from "../../funcs/workflowsEvaluateConversationsByJob.js";
import { EvaluateConversationsByJobRequest$zodSchema } from "../../models/evaluateconversationsbyjobop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EvaluateConversationsByJobRequest$zodSchema,
};

export const tool$workflowsEvaluateConversationsByJob: ToolDefinition<
  typeof args
> = {
  name: "workflows-evaluate-conversations-by-job",
  description: `Trigger evaluations on conversations by evaluation job config

Filters conversations based on job filters, creates an evaluation run, and enqueues workflows`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workflowsEvaluateConversationsByJob(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
