/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectMembersListProjectMembers } from "../../funcs/projectMembersListProjectMembers.js";
import { ListProjectMembersRequest$zodSchema } from "../../models/listprojectmembersop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListProjectMembersRequest$zodSchema,
};

export const tool$projectMembersListProjectMembers: ToolDefinition<
  typeof args
> = {
  name: "project-members-list-project-members",
  description: `List Project Members

List all users with explicit project access. Requires authz to be enabled.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectMembersListProjectMembers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
