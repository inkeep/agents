/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: b6c39f205f67
 */

import { scheduledTriggersRunScheduledTriggerNow } from "../../funcs/scheduledTriggersRunScheduledTriggerNow.js";
import { RunScheduledTriggerNowRequest$zodSchema } from "../../models/runscheduledtriggernowop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RunScheduledTriggerNowRequest$zodSchema,
};

export const tool$scheduledTriggersRunScheduledTriggerNow: ToolDefinition<
  typeof args
> = {
  name: "scheduled-triggers-run-scheduled-trigger-now",
  description: `Run Scheduled Trigger Now`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scheduledTriggersRunScheduledTriggerNow(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
