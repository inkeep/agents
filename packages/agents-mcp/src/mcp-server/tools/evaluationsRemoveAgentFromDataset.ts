/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 4074cd290336
 */

import { evaluationsRemoveAgentFromDataset } from "../../funcs/evaluationsRemoveAgentFromDataset.js";
import { RemoveAgentFromDatasetRequest$zodSchema } from "../../models/removeagentfromdatasetop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RemoveAgentFromDatasetRequest$zodSchema,
};

export const tool$evaluationsRemoveAgentFromDataset: ToolDefinition<
  typeof args
> = {
  name: "evaluations-remove-agent-from-dataset",
  description: `Remove Agent from Dataset`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await evaluationsRemoveAgentFromDataset(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
