/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsAddEvaluatorToJobConfig } from "../../funcs/evaluationsAddEvaluatorToJobConfig.js";
import { AddEvaluatorToJobConfigRequest$zodSchema } from "../../models/addevaluatortojobconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AddEvaluatorToJobConfigRequest$zodSchema,
};

export const tool$evaluationsAddEvaluatorToJobConfig: ToolDefinition<
  typeof args
> = {
  name: "evaluations-add-evaluator-to-job-config",
  description: `Add Evaluator to Evaluation Job Config`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsAddEvaluatorToJobConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
