/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: b1710fa38d7d
 */

import { appsListApps } from "../../funcs/appsListApps.js";
import { ListAppsRequest$zodSchema } from "../../models/listappsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListAppsRequest$zodSchema,
};

export const tool$appsListApps: ToolDefinition<typeof args> = {
  name: "apps-list-apps",
  description: `List Apps

List all app credentials for a project`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await appsListApps(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
