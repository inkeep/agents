/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 1e395656c3bf
 */

import { entitlementsListOrgEntitlements } from "../../funcs/entitlementsListOrgEntitlements.js";
import { ListOrgEntitlementsRequest$zodSchema } from "../../models/listorgentitlementsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListOrgEntitlementsRequest$zodSchema,
};

export const tool$entitlementsListOrgEntitlements: ToolDefinition<typeof args> =
  {
    name: "entitlements-list-org-entitlements",
    description: `List Org Entitlements

List all entitlements for the current organization.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await entitlementsListOrgEntitlements(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
