/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a8d182183b71
 */

import { branchesMergeExecute } from "../../funcs/branchesMergeExecute.js";
import { MergeExecuteRequestRequest$zodSchema } from "../../models/mergeexecuteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: MergeExecuteRequestRequest$zodSchema,
};

export const tool$branchesMergeExecute: ToolDefinition<typeof args> = {
  name: "branches-merge-execute",
  description: `Execute Merge

Execute a merge between two branches with optional conflict resolutions.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await branchesMergeExecute(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
