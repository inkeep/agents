/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0bfd01e0e1df
 */

import { evaluationsListDatasetAgents } from "../../funcs/evaluationsListDatasetAgents.js";
import { ListDatasetAgentsRequest$zodSchema } from "../../models/listdatasetagentsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListDatasetAgentsRequest$zodSchema,
};

export const tool$evaluationsListDatasetAgents: ToolDefinition<typeof args> = {
  name: "evaluations-list-dataset-agents",
  description: `List Agents for Dataset`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await evaluationsListDatasetAgents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
