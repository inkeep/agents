/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0dcc511cc3ba
 */

import { conversationsListConversations } from "../../funcs/conversationsListConversations.js";
import { ListConversationsRequest$zodSchema } from "../../models/listconversationsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListConversationsRequest$zodSchema,
};

export const tool$conversationsListConversations: ToolDefinition<typeof args> =
  {
    name: "conversations-list-conversations",
    description: `List Conversations

List conversations in a project. Supports optional userId filter for viewing a specific end-user's conversations.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await conversationsListConversations(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
