/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { triggersListTriggers } from "../../funcs/triggersListTriggers.js";
import { ListTriggersRequest$zodSchema } from "../../models/listtriggersop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListTriggersRequest$zodSchema,
};

export const tool$triggersListTriggers: ToolDefinition<typeof args> = {
  name: "triggers-list-triggers",
  description: `List Triggers`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await triggersListTriggers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
