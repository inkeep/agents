/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: b79b859e7468
 */

import { skillsDeleteSkillFile } from "../../funcs/skillsDeleteSkillFile.js";
import { DeleteSkillFileRequest$zodSchema } from "../../models/deleteskillfileop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteSkillFileRequest$zodSchema,
};

export const tool$skillsDeleteSkillFile: ToolDefinition<typeof args> = {
  name: "skills-delete-skill-file",
  description: `Delete Skill File`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await skillsDeleteSkillFile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
