/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 158e38e2ec89
 */

import { projectsGetFullProject } from "../../funcs/projectsGetFullProject.js";
import { GetFullProjectRequest$zodSchema } from "../../models/getfullprojectop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetFullProjectRequest$zodSchema,
};

export const tool$projectsGetFullProject: ToolDefinition<typeof args> = {
  name: "projects-get-full-project",
  description: `Get Full Project

Retrieve a complete project definition with all Agents, Sub Agents, tools, and relationships`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectsGetFullProject(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
