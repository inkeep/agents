/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 32b261c23d07
 */

import { gitHubSyncGithubInstallationRepositories } from "../../funcs/gitHubSyncGithubInstallationRepositories.js";
import { SyncGithubInstallationRepositoriesRequest$zodSchema } from "../../models/syncgithubinstallationrepositoriesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SyncGithubInstallationRepositoriesRequest$zodSchema,
};

export const tool$gitHubSyncGithubInstallationRepositories: ToolDefinition<
  typeof args
> = {
  name: "git-hub-sync-github-installation-repositories",
  description: `Sync repositories for a GitHub App installation

Manually refreshes the repository list for a GitHub App installation by fetching the current list from GitHub API. This is useful if webhooks were missed or to ensure the local data is in sync with GitHub.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await gitHubSyncGithubInstallationRepositories(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
