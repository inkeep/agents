/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a9af58ad0220
 */

import { scheduledTriggersCreateScheduledTrigger } from "../../funcs/scheduledTriggersCreateScheduledTrigger.js";
import { CreateScheduledTriggerRequest$zodSchema } from "../../models/createscheduledtriggerop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateScheduledTriggerRequest$zodSchema,
};

export const tool$scheduledTriggersCreateScheduledTrigger: ToolDefinition<
  typeof args
> = {
  name: "scheduled-triggers-create-scheduled-trigger",
  description: `Create Scheduled Trigger`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scheduledTriggersCreateScheduledTrigger(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
