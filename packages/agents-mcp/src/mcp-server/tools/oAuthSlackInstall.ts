/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 66cf27344076
 */

import { oAuthSlackInstall } from "../../funcs/oAuthSlackInstall.js";
import { SlackInstallRequest$zodSchema } from "../../models/slackinstallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SlackInstallRequest$zodSchema.optional(),
};

export const tool$oAuthSlackInstall: ToolDefinition<typeof args> = {
  name: "O-auth-slack-install",
  description: `Install Slack App

Redirects to Slack OAuth page for workspace installation`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oAuthSlackInstall(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
