/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 192ea876fa57
 */

import { authCreateAnonymousSession } from "../../funcs/authCreateAnonymousSession.js";
import { CreateAnonymousSessionRequest$zodSchema } from "../../models/createanonymoussessionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateAnonymousSessionRequest$zodSchema,
};

export const tool$authCreateAnonymousSession: ToolDefinition<typeof args> = {
  name: "auth-create-anonymous-session",
  description: `Create Anonymous Session

Issue an anonymous session JWT for a web_client app. If a valid Bearer token for the same app is provided, the existing anonymous identity is preserved with a fresh expiry (rolling refresh). Otherwise a new identity is created.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await authCreateAnonymousSession(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
