/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a890dd8d95a4
 */

import { workAppsSlackListChannels } from "../../funcs/workAppsSlackListChannels.js";
import { SlackListChannelsRequest$zodSchema } from "../../models/slacklistchannelsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SlackListChannelsRequest$zodSchema,
};

export const tool$workAppsSlackListChannels: ToolDefinition<typeof args> = {
  name: "work-apps-slack-list-channels",
  description: `List Channels

List Slack channels in the workspace that the bot can see`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workAppsSlackListChannels(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
