/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 628faa17663f
 */

import { branchesDeleteBranch } from "../../funcs/branchesDeleteBranch.js";
import { DeleteBranchRequest$zodSchema } from "../../models/deletebranchop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteBranchRequest$zodSchema,
};

export const tool$branchesDeleteBranch: ToolDefinition<typeof args> = {
  name: "branches-delete-branch",
  description: `Delete Branch

Delete a branch. Cannot delete protected branches like main.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await branchesDeleteBranch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
