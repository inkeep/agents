/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ae84e1eef26f
 */

import { subAgentsUpdateSubagent } from "../../funcs/subAgentsUpdateSubagent.js";
import { UpdateSubagentRequest$zodSchema } from "../../models/updatesubagentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateSubagentRequest$zodSchema,
};

export const tool$subAgentsUpdateSubagent: ToolDefinition<typeof args> = {
  name: "sub-agents-update-subagent",
  description: `Update SubAgent`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await subAgentsUpdateSubagent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
