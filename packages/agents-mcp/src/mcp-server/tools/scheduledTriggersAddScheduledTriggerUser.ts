/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: e47c9693e182
 */

import { scheduledTriggersAddScheduledTriggerUser } from "../../funcs/scheduledTriggersAddScheduledTriggerUser.js";
import { AddScheduledTriggerUserRequestRequest$zodSchema } from "../../models/addscheduledtriggeruserop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AddScheduledTriggerUserRequestRequest$zodSchema,
};

export const tool$scheduledTriggersAddScheduledTriggerUser: ToolDefinition<
  typeof args
> = {
  name: "scheduled-triggers-add-scheduled-trigger-user",
  description: `Add User to Scheduled Trigger`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await scheduledTriggersAddScheduledTriggerUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
