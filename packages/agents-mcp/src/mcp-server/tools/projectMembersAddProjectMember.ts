/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: c5de62e91552
 */

import { projectMembersAddProjectMember } from "../../funcs/projectMembersAddProjectMember.js";
import { AddProjectMemberRequest$zodSchema } from "../../models/addprojectmemberop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AddProjectMemberRequest$zodSchema,
};

export const tool$projectMembersAddProjectMember: ToolDefinition<typeof args> =
  {
    name: "project-members-add-project-member",
    description: `Add Project Member

Add a user to a project with a specified role. Requires authz to be enabled.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await projectMembersAddProjectMember(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
