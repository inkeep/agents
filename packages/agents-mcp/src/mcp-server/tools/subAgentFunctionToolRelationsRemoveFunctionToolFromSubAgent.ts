/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subAgentFunctionToolRelationsRemoveFunctionToolFromSubAgent } from "../../funcs/subAgentFunctionToolRelationsRemoveFunctionToolFromSubAgent.js";
import { RemoveFunctionToolFromSubAgentRequest$zodSchema } from "../../models/removefunctiontoolfromsubagentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RemoveFunctionToolFromSubAgentRequest$zodSchema,
};

export const tool$subAgentFunctionToolRelationsRemoveFunctionToolFromSubAgent:
  ToolDefinition<typeof args> = {
    name:
      "sub-agent-function-tool-relations-remove-function-tool-from-sub-agent",
    description: `Remove Function Tool from SubAgent`,
    annotations: {
      "title": "",
      "destructiveHint": true,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await subAgentFunctionToolRelationsRemoveFunctionToolFromSubAgent(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
