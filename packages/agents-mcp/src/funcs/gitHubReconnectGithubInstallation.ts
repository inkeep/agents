/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 06fc78590b21
 */

import { InkeepAgentsCore } from "../core.js";
import { encodeSimple } from "../lib/encodings.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { APIError } from "../models/errors/apierror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import {
  ReconnectGithubInstallationRequest,
  ReconnectGithubInstallationRequest$zodSchema,
  ReconnectGithubInstallationResponse,
  ReconnectGithubInstallationResponse$zodSchema,
} from "../models/reconnectgithubinstallationop.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

export enum ReconnectGithubInstallationAcceptEnum {
  applicationJsonAccept = "application/json",
  applicationProblemPlusJsonAccept = "application/problem+json",
}

/**
 * Reconnect a disconnected GitHub App installation
 *
 * @remarks
 * Reconnects a previously disconnected GitHub App installation by setting its status back to "active" and syncing the available repositories from GitHub. This can only be used on installations with "disconnected" status.
 */
export function gitHubReconnectGithubInstallation(
  client$: InkeepAgentsCore,
  request: ReconnectGithubInstallationRequest,
  options?: RequestOptions,
): APIPromise<
  Result<
    ReconnectGithubInstallationResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >
> {
  return new APIPromise($do(
    client$,
    request,
    options,
  ));
}

async function $do(
  client$: InkeepAgentsCore,
  request: ReconnectGithubInstallationRequest,
  options?: RequestOptions & {
    acceptHeaderOverride?: ReconnectGithubInstallationAcceptEnum;
  },
): Promise<
  [
    Result<
      ReconnectGithubInstallationResponse,
      | APIError
      | SDKValidationError
      | UnexpectedClientError
      | InvalidRequestError
      | RequestAbortedError
      | RequestTimeoutError
      | ConnectionError
    >,
    APICall,
  ]
> {
  const parsed$ = safeParse(
    request,
    (value$) => ReconnectGithubInstallationRequest$zodSchema.parse(value$),
    "Input validation failed",
  );
  if (!parsed$.ok) {
    return [parsed$, { status: "invalid" }];
  }
  const payload$ = parsed$.value;
  const body$ = null;

  const pathParams$ = {
    installationId: encodeSimple("installationId", payload$.installationId, {
      explode: false,
      charEncoding: "percent",
    }),
    tenantId: encodeSimple("tenantId", payload$.tenantId, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path$ = pathToFunc(
    "/manage/tenants/{tenantId}/github/installations/{installationId}/reconnect",
  )(
    pathParams$,
  );

  const headers$ = new Headers(compactMap({
    Accept: options?.acceptHeaderOverride
      || "application/json;q=1, application/problem+json;q=0",
  }));
  const securityInput = await extractSecurity(client$._options.security);
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client$._options,
    baseURL: options?.serverURL ?? client$._baseURL ?? "",
    operationID: "reconnect-github-installation",
    oAuth2Scopes: null,
    resolvedSecurity: requestSecurity,
    securitySource: client$._options.security,
    retryConfig: options?.retries
      || client$._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || [
      "429",
      "500",
      "502",
      "503",
      "504",
    ],
  };

  const requestRes = client$._createRequest(context, {
    security: requestSecurity,
    method: "POST",
    baseURL: options?.serverURL,
    path: path$,
    headers: headers$,
    body: body$,
    userAgent: client$._options.userAgent,
    timeoutMs: options?.timeoutMs || client$._options.timeoutMs
      || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req$ = requestRes.value;

  const doResult = await client$._do(req$, {
    context,
    errorCodes: [],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req$ }];
  }
  const response = doResult.value;
  const responseFields$ = {
    HttpMeta: { Response: response, Request: req$ },
  };

  const [result$] = await M.match<
    ReconnectGithubInstallationResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >(
    M.json(200, ReconnectGithubInstallationResponse$zodSchema, {
      key: "twoHundredApplicationJsonObject",
    }),
    M.json(400, ReconnectGithubInstallationResponse$zodSchema, {
      ctype: "application/problem+json",
      key: "BadRequest",
    }),
    M.json(401, ReconnectGithubInstallationResponse$zodSchema, {
      ctype: "application/problem+json",
      key: "Unauthorized",
    }),
    M.json(403, ReconnectGithubInstallationResponse$zodSchema, {
      ctype: "application/problem+json",
      key: "Forbidden",
    }),
    M.json(422, ReconnectGithubInstallationResponse$zodSchema, {
      ctype: "application/problem+json",
      key: "UnprocessableEntity",
    }),
    M.json(500, ReconnectGithubInstallationResponse$zodSchema, {
      ctype: "application/problem+json",
      key: "InternalServerError",
    }),
    M.json(503, ReconnectGithubInstallationResponse$zodSchema, {
      ctype: "application/problem+json",
      key: "fiveHundredAndThreeApplicationProblemPlusJsonObject",
    }),
  )(response, req$, { extraFields: responseFields$ });

  return [result$, { status: "complete", request: req$, response }];
}
