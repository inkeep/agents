/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { InkeepAgentsCore } from '../core.js';
import { encodeSimple } from '../lib/encodings.js';
import * as M from '../lib/matchers.js';
import { compactMap } from '../lib/primitives.js';
import { safeParse } from '../lib/schemas.js';
import { RequestOptions } from '../lib/sdks.js';
import { pathToFunc } from '../lib/url.js';
import {
  AddEvaluatorToSuiteConfigRequest,
  AddEvaluatorToSuiteConfigRequest$zodSchema,
  AddEvaluatorToSuiteConfigResponse,
  AddEvaluatorToSuiteConfigResponse$zodSchema,
} from '../models/addevaluatortosuiteconfigop.js';
import { APIError } from '../models/errors/apierror.js';
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from '../models/errors/httpclienterrors.js';
import { SDKValidationError } from '../models/errors/sdkvalidationerror.js';
import { APICall, APIPromise } from '../types/async.js';
import { Result } from '../types/fp.js';

export enum AddEvaluatorToSuiteConfigAcceptEnum {
  applicationJsonAccept = 'application/json',
  applicationProblemPlusJsonAccept = 'application/problem+json',
}

/**
 * Add Evaluator to Evaluation Suite Config
 */
export function evaluationsAddEvaluatorToSuiteConfig(
  client$: InkeepAgentsCore,
  request: AddEvaluatorToSuiteConfigRequest,
  options?: RequestOptions
): APIPromise<
  Result<
    AddEvaluatorToSuiteConfigResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >
> {
  return new APIPromise($do(client$, request, options));
}

async function $do(
  client$: InkeepAgentsCore,
  request: AddEvaluatorToSuiteConfigRequest,
  options?: RequestOptions & {
    acceptHeaderOverride?: AddEvaluatorToSuiteConfigAcceptEnum;
  }
): Promise<
  [
    Result<
      AddEvaluatorToSuiteConfigResponse,
      | APIError
      | SDKValidationError
      | UnexpectedClientError
      | InvalidRequestError
      | RequestAbortedError
      | RequestTimeoutError
      | ConnectionError
    >,
    APICall,
  ]
> {
  const parsed$ = safeParse(
    request,
    (value$) => AddEvaluatorToSuiteConfigRequest$zodSchema.parse(value$),
    'Input validation failed'
  );
  if (!parsed$.ok) {
    return [parsed$, { status: 'invalid' }];
  }
  const payload$ = parsed$.value;
  const body$ = null;

  const pathParams$ = {
    configId: encodeSimple('configId', payload$.configId, {
      explode: false,
      charEncoding: 'percent',
    }),
    evaluatorId: encodeSimple('evaluatorId', payload$.evaluatorId, {
      explode: false,
      charEncoding: 'percent',
    }),
    projectId: encodeSimple('projectId', payload$.projectId, {
      explode: false,
      charEncoding: 'percent',
    }),
    tenantId: encodeSimple('tenantId', payload$.tenantId, {
      explode: false,
      charEncoding: 'percent',
    }),
  };
  const path$ = pathToFunc(
    '/tenants/{tenantId}/projects/{projectId}/evaluations/evaluation-suite-configs/{configId}/evaluators/{evaluatorId}'
  )(pathParams$);

  const headers$ = new Headers(
    compactMap({
      Accept: options?.acceptHeaderOverride || 'application/json;q=1, application/problem+json;q=0',
    })
  );

  const context = {
    options: client$._options,
    baseURL: options?.serverURL ?? client$._baseURL ?? '',
    operationID: 'add-evaluator-to-suite-config',
    oAuth2Scopes: null,
    resolvedSecurity: null,
    securitySource: null,
    retryConfig: options?.retries || client$._options.retryConfig || { strategy: 'none' },
    retryCodes: options?.retryCodes || ['429', '500', '502', '503', '504'],
  };

  const requestRes = client$._createRequest(
    context,
    {
      method: 'POST',
      baseURL: options?.serverURL,
      path: path$,
      headers: headers$,
      body: body$,
      userAgent: client$._options.userAgent,
      timeoutMs: options?.timeoutMs || client$._options.timeoutMs || -1,
    },
    options
  );
  if (!requestRes.ok) {
    return [requestRes, { status: 'invalid' }];
  }
  const req$ = requestRes.value;

  const doResult = await client$._do(req$, {
    context,
    errorCodes: [],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: 'request-error', request: req$ }];
  }
  const response = doResult.value;
  const responseFields$ = {
    HttpMeta: { Response: response, Request: req$ },
  };

  const [result$] = await M.match<
    AddEvaluatorToSuiteConfigResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >(
    M.json(201, AddEvaluatorToSuiteConfigResponse$zodSchema, { key: 'any' }),
    M.json(400, AddEvaluatorToSuiteConfigResponse$zodSchema, {
      ctype: 'application/problem+json',
      key: 'BadRequest',
    }),
    M.json(401, AddEvaluatorToSuiteConfigResponse$zodSchema, {
      ctype: 'application/problem+json',
      key: 'Unauthorized',
    }),
    M.json(403, AddEvaluatorToSuiteConfigResponse$zodSchema, {
      ctype: 'application/problem+json',
      key: 'Forbidden',
    }),
    M.json(404, AddEvaluatorToSuiteConfigResponse$zodSchema, {
      ctype: 'application/problem+json',
      key: 'NotFound',
    }),
    M.json(422, AddEvaluatorToSuiteConfigResponse$zodSchema, {
      ctype: 'application/problem+json',
      key: 'UnprocessableEntity',
    }),
    M.json(500, AddEvaluatorToSuiteConfigResponse$zodSchema, {
      ctype: 'application/problem+json',
      key: 'InternalServerError',
    })
  )(response, req$, { extraFields: responseFields$ });

  return [result$, { status: 'complete', request: req$, response }];
}
