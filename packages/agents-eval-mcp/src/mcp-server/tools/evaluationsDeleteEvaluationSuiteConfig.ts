/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsDeleteEvaluationSuiteConfig } from '../../funcs/evaluationsDeleteEvaluationSuiteConfig.js';
import { DeleteEvaluationSuiteConfigRequest$zodSchema } from '../../models/deleteevaluationsuiteconfigop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: DeleteEvaluationSuiteConfigRequest$zodSchema,
};

export const tool$evaluationsDeleteEvaluationSuiteConfig: ToolDefinition<typeof args> = {
  name: 'evaluations-delete-evaluation-suite-config',
  description: `Delete Evaluation Suite Config`,
  annotations: {
    title: '',
    destructiveHint: true,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsDeleteEvaluationSuiteConfig(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
