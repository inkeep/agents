/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsListDatasetRuns } from '../../funcs/evaluationsListDatasetRuns.js';
import { ListDatasetRunsRequest$zodSchema } from '../../models/listdatasetrunsop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: ListDatasetRunsRequest$zodSchema,
};

export const tool$evaluationsListDatasetRuns: ToolDefinition<typeof args> = {
  name: 'evaluations-list-dataset-runs',
  description: `List Dataset Runs`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsListDatasetRuns(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
