/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsGetEvaluationResult } from '../../funcs/evaluationsGetEvaluationResult.js';
import { GetEvaluationResultRequest$zodSchema } from '../../models/getevaluationresultop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: GetEvaluationResultRequest$zodSchema,
};

export const tool$evaluationsGetEvaluationResult: ToolDefinition<typeof args> = {
  name: 'evaluations-get-evaluation-result',
  description: `Get Evaluation Result by ID`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsGetEvaluationResult(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
