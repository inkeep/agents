/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsListEvaluationSuiteConfigs } from '../../funcs/evaluationsListEvaluationSuiteConfigs.js';
import { ListEvaluationSuiteConfigsRequest$zodSchema } from '../../models/listevaluationsuiteconfigsop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: ListEvaluationSuiteConfigsRequest$zodSchema,
};

export const tool$evaluationsListEvaluationSuiteConfigs: ToolDefinition<typeof args> = {
  name: 'evaluations-list-evaluation-suite-configs',
  description: `List Evaluation Suite Configs`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsListEvaluationSuiteConfigs(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
