/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsGetEvaluationJobConfig } from '../../funcs/evaluationsGetEvaluationJobConfig.js';
import { GetEvaluationJobConfigRequest$zodSchema } from '../../models/getevaluationjobconfigop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: GetEvaluationJobConfigRequest$zodSchema,
};

export const tool$evaluationsGetEvaluationJobConfig: ToolDefinition<typeof args> = {
  name: 'evaluations-get-evaluation-job-config',
  description: `Get Evaluation Job Config by ID`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsGetEvaluationJobConfig(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
