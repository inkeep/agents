/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsGetEvaluator } from '../../funcs/evaluationsGetEvaluator.js';
import { GetEvaluatorRequest$zodSchema } from '../../models/getevaluatorop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: GetEvaluatorRequest$zodSchema,
};

export const tool$evaluationsGetEvaluator: ToolDefinition<typeof args> = {
  name: 'evaluations-get-evaluator',
  description: `Get Evaluator by ID`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsGetEvaluator(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
