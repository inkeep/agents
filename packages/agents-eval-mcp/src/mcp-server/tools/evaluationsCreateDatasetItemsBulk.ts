/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsCreateDatasetItemsBulk } from '../../funcs/evaluationsCreateDatasetItemsBulk.js';
import { CreateDatasetItemsBulkRequest$zodSchema } from '../../models/createdatasetitemsbulkop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: CreateDatasetItemsBulkRequest$zodSchema,
};

export const tool$evaluationsCreateDatasetItemsBulk: ToolDefinition<typeof args> = {
  name: 'evaluations-create-dataset-items-bulk',
  description: `Create Multiple Dataset Items`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsCreateDatasetItemsBulk(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
