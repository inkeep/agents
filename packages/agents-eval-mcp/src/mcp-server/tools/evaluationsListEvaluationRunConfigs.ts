/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsListEvaluationRunConfigs } from '../../funcs/evaluationsListEvaluationRunConfigs.js';
import { ListEvaluationRunConfigsRequest$zodSchema } from '../../models/listevaluationrunconfigsop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: ListEvaluationRunConfigsRequest$zodSchema,
};

export const tool$evaluationsListEvaluationRunConfigs: ToolDefinition<typeof args> = {
  name: 'evaluations-list-evaluation-run-configs',
  description: `List Evaluation Run Configs`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsListEvaluationRunConfigs(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
