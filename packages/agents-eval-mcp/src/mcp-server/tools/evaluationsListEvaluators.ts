/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsListEvaluators } from '../../funcs/evaluationsListEvaluators.js';
import { ListEvaluatorsRequest$zodSchema } from '../../models/listevaluatorsop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: ListEvaluatorsRequest$zodSchema,
};

export const tool$evaluationsListEvaluators: ToolDefinition<typeof args> = {
  name: 'evaluations-list-evaluators',
  description: `List Evaluators`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsListEvaluators(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
