/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsDeleteEvaluationJobConfig } from '../../funcs/evaluationsDeleteEvaluationJobConfig.js';
import { DeleteEvaluationJobConfigRequest$zodSchema } from '../../models/deleteevaluationjobconfigop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: DeleteEvaluationJobConfigRequest$zodSchema,
};

export const tool$evaluationsDeleteEvaluationJobConfig: ToolDefinition<typeof args> = {
  name: 'evaluations-delete-evaluation-job-config',
  description: `Delete Evaluation Job Config`,
  annotations: {
    title: '',
    destructiveHint: true,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsDeleteEvaluationJobConfig(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
