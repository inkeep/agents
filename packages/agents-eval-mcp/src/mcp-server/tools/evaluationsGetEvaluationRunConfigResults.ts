/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsGetEvaluationRunConfigResults } from '../../funcs/evaluationsGetEvaluationRunConfigResults.js';
import { GetEvaluationRunConfigResultsRequest$zodSchema } from '../../models/getevaluationrunconfigresultsop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: GetEvaluationRunConfigResultsRequest$zodSchema,
};

export const tool$evaluationsGetEvaluationRunConfigResults: ToolDefinition<typeof args> = {
  name: 'evaluations-get-evaluation-run-config-results',
  description: `Get Evaluation Results by Run Config ID`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsGetEvaluationRunConfigResults(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
