/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsUpdateEvaluationSuiteConfig } from '../../funcs/evaluationsUpdateEvaluationSuiteConfig.js';
import { UpdateEvaluationSuiteConfigRequest$zodSchema } from '../../models/updateevaluationsuiteconfigop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: UpdateEvaluationSuiteConfigRequest$zodSchema,
};

export const tool$evaluationsUpdateEvaluationSuiteConfig: ToolDefinition<typeof args> = {
  name: 'evaluations-update-evaluation-suite-config',
  description: `Update Evaluation Suite Config`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsUpdateEvaluationSuiteConfig(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
