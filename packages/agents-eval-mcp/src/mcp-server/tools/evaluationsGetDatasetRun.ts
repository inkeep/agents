/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsGetDatasetRun } from '../../funcs/evaluationsGetDatasetRun.js';
import { GetDatasetRunRequest$zodSchema } from '../../models/getdatasetrunop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: GetDatasetRunRequest$zodSchema,
};

export const tool$evaluationsGetDatasetRun: ToolDefinition<typeof args> = {
  name: 'evaluations-get-dataset-run',
  description: `Get Dataset Run`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsGetDatasetRun(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
