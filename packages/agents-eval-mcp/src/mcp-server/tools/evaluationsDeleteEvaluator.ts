/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsDeleteEvaluator } from '../../funcs/evaluationsDeleteEvaluator.js';
import { DeleteEvaluatorRequest$zodSchema } from '../../models/deleteevaluatorop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: DeleteEvaluatorRequest$zodSchema,
};

export const tool$evaluationsDeleteEvaluator: ToolDefinition<typeof args> = {
  name: 'evaluations-delete-evaluator',
  description: `Delete Evaluator`,
  annotations: {
    title: '',
    destructiveHint: true,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsDeleteEvaluator(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
