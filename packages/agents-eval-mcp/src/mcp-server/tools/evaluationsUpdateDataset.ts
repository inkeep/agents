/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsUpdateDataset } from '../../funcs/evaluationsUpdateDataset.js';
import { UpdateDatasetRequest$zodSchema } from '../../models/updatedatasetop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: UpdateDatasetRequest$zodSchema,
};

export const tool$evaluationsUpdateDataset: ToolDefinition<typeof args> = {
  name: 'evaluations-update-dataset',
  description: `Update Dataset`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsUpdateDataset(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
