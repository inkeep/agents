/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsCreateDatasetRunConfig } from '../../funcs/evaluationsCreateDatasetRunConfig.js';
import { CreateDatasetRunConfigRequest$zodSchema } from '../../models/createdatasetrunconfigop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: CreateDatasetRunConfigRequest$zodSchema,
};

export const tool$evaluationsCreateDatasetRunConfig: ToolDefinition<typeof args> = {
  name: 'evaluations-create-dataset-run-config',
  description: `Create Dataset Run Config`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsCreateDatasetRunConfig(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
