/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsCreateEvaluationResult } from '../../funcs/evaluationsCreateEvaluationResult.js';
import { CreateEvaluationResultRequest$zodSchema } from '../../models/createevaluationresultop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: CreateEvaluationResultRequest$zodSchema,
};

export const tool$evaluationsCreateEvaluationResult: ToolDefinition<typeof args> = {
  name: 'evaluations-create-evaluation-result',
  description: `Create Evaluation Result`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsCreateEvaluationResult(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
