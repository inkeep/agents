/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsListEvaluationJobConfigs } from '../../funcs/evaluationsListEvaluationJobConfigs.js';
import { ListEvaluationJobConfigsRequest$zodSchema } from '../../models/listevaluationjobconfigsop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: ListEvaluationJobConfigsRequest$zodSchema,
};

export const tool$evaluationsListEvaluationJobConfigs: ToolDefinition<typeof args> = {
  name: 'evaluations-list-evaluation-job-configs',
  description: `List Evaluation Job Configs`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsListEvaluationJobConfigs(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
