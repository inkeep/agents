/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsCreateEvaluator } from '../../funcs/evaluationsCreateEvaluator.js';
import { CreateEvaluatorRequest$zodSchema } from '../../models/createevaluatorop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: CreateEvaluatorRequest$zodSchema,
};

export const tool$evaluationsCreateEvaluator: ToolDefinition<typeof args> = {
  name: 'evaluations-create-evaluator',
  description: `Create Evaluator`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsCreateEvaluator(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
