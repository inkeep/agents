/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsCreateDataset } from '../../funcs/evaluationsCreateDataset.js';
import { CreateDatasetRequest$zodSchema } from '../../models/createdatasetop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: CreateDatasetRequest$zodSchema,
};

export const tool$evaluationsCreateDataset: ToolDefinition<typeof args> = {
  name: 'evaluations-create-dataset',
  description: `Create Dataset`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsCreateDataset(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
