/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsGetEvaluationRunConfig } from '../../funcs/evaluationsGetEvaluationRunConfig.js';
import { GetEvaluationRunConfigRequest$zodSchema } from '../../models/getevaluationrunconfigop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: GetEvaluationRunConfigRequest$zodSchema,
};

export const tool$evaluationsGetEvaluationRunConfig: ToolDefinition<typeof args> = {
  name: 'evaluations-get-evaluation-run-config',
  description: `Get Evaluation Run Config by ID`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsGetEvaluationRunConfig(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
