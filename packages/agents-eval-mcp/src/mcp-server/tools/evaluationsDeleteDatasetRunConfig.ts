/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsDeleteDatasetRunConfig } from '../../funcs/evaluationsDeleteDatasetRunConfig.js';
import { DeleteDatasetRunConfigRequest$zodSchema } from '../../models/deletedatasetrunconfigop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: DeleteDatasetRunConfigRequest$zodSchema,
};

export const tool$evaluationsDeleteDatasetRunConfig: ToolDefinition<typeof args> = {
  name: 'evaluations-delete-dataset-run-config',
  description: `Delete Dataset Run Config`,
  annotations: {
    title: '',
    destructiveHint: true,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsDeleteDatasetRunConfig(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
