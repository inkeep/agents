/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsListDatasets } from '../../funcs/evaluationsListDatasets.js';
import { ListDatasetsRequest$zodSchema } from '../../models/listdatasetsop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: ListDatasetsRequest$zodSchema,
};

export const tool$evaluationsListDatasets: ToolDefinition<typeof args> = {
  name: 'evaluations-list-datasets',
  description: `List Datasets`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsListDatasets(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
