/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsDeleteDataset } from '../../funcs/evaluationsDeleteDataset.js';
import { DeleteDatasetRequest$zodSchema } from '../../models/deletedatasetop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: DeleteDatasetRequest$zodSchema,
};

export const tool$evaluationsDeleteDataset: ToolDefinition<typeof args> = {
  name: 'evaluations-delete-dataset',
  description: `Delete Dataset`,
  annotations: {
    title: '',
    destructiveHint: true,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsDeleteDataset(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
