/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsGetDataset } from '../../funcs/evaluationsGetDataset.js';
import { GetDatasetRequest$zodSchema } from '../../models/getdatasetop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: GetDatasetRequest$zodSchema,
};

export const tool$evaluationsGetDataset: ToolDefinition<typeof args> = {
  name: 'evaluations-get-dataset',
  description: `Get Dataset by ID`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsGetDataset(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
