/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsUpdateDatasetRunConfig } from '../../funcs/evaluationsUpdateDatasetRunConfig.js';
import { UpdateDatasetRunConfigRequest$zodSchema } from '../../models/updatedatasetrunconfigop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: UpdateDatasetRunConfigRequest$zodSchema,
};

export const tool$evaluationsUpdateDatasetRunConfig: ToolDefinition<typeof args> = {
  name: 'evaluations-update-dataset-run-config',
  description: `Update Dataset Run Config`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsUpdateDatasetRunConfig(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
