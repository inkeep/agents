/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsGetDatasetItem } from '../../funcs/evaluationsGetDatasetItem.js';
import { GetDatasetItemRequest$zodSchema } from '../../models/getdatasetitemop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: GetDatasetItemRequest$zodSchema,
};

export const tool$evaluationsGetDatasetItem: ToolDefinition<typeof args> = {
  name: 'evaluations-get-dataset-item',
  description: `Get Dataset Item by ID`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsGetDatasetItem(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
