/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsListDatasetRunConfigs } from '../../funcs/evaluationsListDatasetRunConfigs.js';
import { ListDatasetRunConfigsRequest$zodSchema } from '../../models/listdatasetrunconfigsop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: ListDatasetRunConfigsRequest$zodSchema,
};

export const tool$evaluationsListDatasetRunConfigs: ToolDefinition<typeof args> = {
  name: 'evaluations-list-dataset-run-configs',
  description: `List Dataset Run Configs`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsListDatasetRunConfigs(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
