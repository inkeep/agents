/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsDeleteDatasetItem } from '../../funcs/evaluationsDeleteDatasetItem.js';
import { DeleteDatasetItemRequest$zodSchema } from '../../models/deletedatasetitemop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: DeleteDatasetItemRequest$zodSchema,
};

export const tool$evaluationsDeleteDatasetItem: ToolDefinition<typeof args> = {
  name: 'evaluations-delete-dataset-item',
  description: `Delete Dataset Item`,
  annotations: {
    title: '',
    destructiveHint: true,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsDeleteDatasetItem(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
