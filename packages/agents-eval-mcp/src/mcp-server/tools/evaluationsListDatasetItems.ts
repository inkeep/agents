/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsListDatasetItems } from '../../funcs/evaluationsListDatasetItems.js';
import { ListDatasetItemsRequest$zodSchema } from '../../models/listdatasetitemsop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: ListDatasetItemsRequest$zodSchema,
};

export const tool$evaluationsListDatasetItems: ToolDefinition<typeof args> = {
  name: 'evaluations-list-dataset-items',
  description: `List Dataset Items`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsListDatasetItems(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
