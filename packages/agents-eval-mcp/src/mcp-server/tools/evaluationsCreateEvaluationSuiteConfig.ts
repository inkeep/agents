/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsCreateEvaluationSuiteConfig } from '../../funcs/evaluationsCreateEvaluationSuiteConfig.js';
import { CreateEvaluationSuiteConfigRequest$zodSchema } from '../../models/createevaluationsuiteconfigop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: CreateEvaluationSuiteConfigRequest$zodSchema,
};

export const tool$evaluationsCreateEvaluationSuiteConfig: ToolDefinition<typeof args> = {
  name: 'evaluations-create-evaluation-suite-config',
  description: `Create Evaluation Suite Config`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsCreateEvaluationSuiteConfig(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
