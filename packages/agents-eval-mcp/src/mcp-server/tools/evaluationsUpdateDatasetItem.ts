/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsUpdateDatasetItem } from '../../funcs/evaluationsUpdateDatasetItem.js';
import { UpdateDatasetItemRequest$zodSchema } from '../../models/updatedatasetitemop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: UpdateDatasetItemRequest$zodSchema,
};

export const tool$evaluationsUpdateDatasetItem: ToolDefinition<typeof args> = {
  name: 'evaluations-update-dataset-item',
  description: `Update Dataset Item`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsUpdateDatasetItem(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
