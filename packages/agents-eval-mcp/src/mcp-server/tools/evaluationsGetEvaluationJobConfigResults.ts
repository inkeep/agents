/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsGetEvaluationJobConfigResults } from '../../funcs/evaluationsGetEvaluationJobConfigResults.js';
import { GetEvaluationJobConfigResultsRequest$zodSchema } from '../../models/getevaluationjobconfigresultsop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: GetEvaluationJobConfigResultsRequest$zodSchema,
};

export const tool$evaluationsGetEvaluationJobConfigResults: ToolDefinition<typeof args> = {
  name: 'evaluations-get-evaluation-job-config-results',
  description: `Get Evaluation Results by Job Config ID`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsGetEvaluationJobConfigResults(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
