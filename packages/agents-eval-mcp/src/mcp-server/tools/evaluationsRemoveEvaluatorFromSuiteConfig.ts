/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsRemoveEvaluatorFromSuiteConfig } from '../../funcs/evaluationsRemoveEvaluatorFromSuiteConfig.js';
import { RemoveEvaluatorFromSuiteConfigRequest$zodSchema } from '../../models/removeevaluatorfromsuiteconfigop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: RemoveEvaluatorFromSuiteConfigRequest$zodSchema,
};

export const tool$evaluationsRemoveEvaluatorFromSuiteConfig: ToolDefinition<typeof args> = {
  name: 'evaluations-remove-evaluator-from-suite-config',
  description: `Remove Evaluator from Evaluation Suite Config`,
  annotations: {
    title: '',
    destructiveHint: true,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsRemoveEvaluatorFromSuiteConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } }
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
