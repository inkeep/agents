/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsUpdateEvaluationResult } from '../../funcs/evaluationsUpdateEvaluationResult.js';
import { UpdateEvaluationResultRequest$zodSchema } from '../../models/updateevaluationresultop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: UpdateEvaluationResultRequest$zodSchema,
};

export const tool$evaluationsUpdateEvaluationResult: ToolDefinition<typeof args> = {
  name: 'evaluations-update-evaluation-result',
  description: `Update Evaluation Result`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsUpdateEvaluationResult(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
