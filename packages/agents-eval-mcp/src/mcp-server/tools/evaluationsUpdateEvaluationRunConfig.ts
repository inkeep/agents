/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsUpdateEvaluationRunConfig } from '../../funcs/evaluationsUpdateEvaluationRunConfig.js';
import { UpdateEvaluationRunConfigRequest$zodSchema } from '../../models/updateevaluationrunconfigop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: UpdateEvaluationRunConfigRequest$zodSchema,
};

export const tool$evaluationsUpdateEvaluationRunConfig: ToolDefinition<typeof args> = {
  name: 'evaluations-update-evaluation-run-config',
  description: `Update Evaluation Run Config`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsUpdateEvaluationRunConfig(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
