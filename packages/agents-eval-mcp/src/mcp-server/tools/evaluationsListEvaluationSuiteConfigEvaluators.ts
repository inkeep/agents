/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsListEvaluationSuiteConfigEvaluators } from '../../funcs/evaluationsListEvaluationSuiteConfigEvaluators.js';
import { ListEvaluationSuiteConfigEvaluatorsRequest$zodSchema } from '../../models/listevaluationsuiteconfigevaluatorsop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: ListEvaluationSuiteConfigEvaluatorsRequest$zodSchema,
};

export const tool$evaluationsListEvaluationSuiteConfigEvaluators: ToolDefinition<typeof args> = {
  name: 'evaluations-list-evaluation-suite-config-evaluators',
  description: `List Evaluators for Evaluation Suite Config`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsListEvaluationSuiteConfigEvaluators(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } }
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
