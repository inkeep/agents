/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsGetEvaluationSuiteConfig } from '../../funcs/evaluationsGetEvaluationSuiteConfig.js';
import { GetEvaluationSuiteConfigRequest$zodSchema } from '../../models/getevaluationsuiteconfigop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: GetEvaluationSuiteConfigRequest$zodSchema,
};

export const tool$evaluationsGetEvaluationSuiteConfig: ToolDefinition<typeof args> = {
  name: 'evaluations-get-evaluation-suite-config',
  description: `Get Evaluation Suite Config by ID`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsGetEvaluationSuiteConfig(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
