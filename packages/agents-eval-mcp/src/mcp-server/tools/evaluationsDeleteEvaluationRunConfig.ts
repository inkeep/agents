/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsDeleteEvaluationRunConfig } from '../../funcs/evaluationsDeleteEvaluationRunConfig.js';
import { DeleteEvaluationRunConfigRequest$zodSchema } from '../../models/deleteevaluationrunconfigop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: DeleteEvaluationRunConfigRequest$zodSchema,
};

export const tool$evaluationsDeleteEvaluationRunConfig: ToolDefinition<typeof args> = {
  name: 'evaluations-delete-evaluation-run-config',
  description: `Delete Evaluation Run Config`,
  annotations: {
    title: '',
    destructiveHint: true,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsDeleteEvaluationRunConfig(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
