/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsAddEvaluatorToSuiteConfig } from '../../funcs/evaluationsAddEvaluatorToSuiteConfig.js';
import { AddEvaluatorToSuiteConfigRequest$zodSchema } from '../../models/addevaluatortosuiteconfigop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: AddEvaluatorToSuiteConfigRequest$zodSchema,
};

export const tool$evaluationsAddEvaluatorToSuiteConfig: ToolDefinition<typeof args> = {
  name: 'evaluations-add-evaluator-to-suite-config',
  description: `Add Evaluator to Evaluation Suite Config`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsAddEvaluatorToSuiteConfig(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
