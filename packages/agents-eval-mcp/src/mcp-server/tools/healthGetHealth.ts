/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { healthGetHealth } from '../../funcs/healthGetHealth.js';
import { formatResult, ToolDefinition } from '../tools.js';

export const tool$healthGetHealth: ToolDefinition = {
  name: 'health-get-health',
  description: `Health check

Check if the evaluation service is healthy`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: true,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await healthGetHealth(client, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
