/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsCreateEvaluationRunConfig } from '../../funcs/evaluationsCreateEvaluationRunConfig.js';
import { CreateEvaluationRunConfigRequest$zodSchema } from '../../models/createevaluationrunconfigop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: CreateEvaluationRunConfigRequest$zodSchema,
};

export const tool$evaluationsCreateEvaluationRunConfig: ToolDefinition<typeof args> = {
  name: 'evaluations-create-evaluation-run-config',
  description: `Create Evaluation Run Config`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsCreateEvaluationRunConfig(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
