/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsUpdateEvaluator } from '../../funcs/evaluationsUpdateEvaluator.js';
import { UpdateEvaluatorRequest$zodSchema } from '../../models/updateevaluatorop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: UpdateEvaluatorRequest$zodSchema,
};

export const tool$evaluationsUpdateEvaluator: ToolDefinition<typeof args> = {
  name: 'evaluations-update-evaluator',
  description: `Update Evaluator`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsUpdateEvaluator(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
