/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evaluationsCreateDatasetItem } from '../../funcs/evaluationsCreateDatasetItem.js';
import { CreateDatasetItemRequest$zodSchema } from '../../models/createdatasetitemop.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: CreateDatasetItemRequest$zodSchema,
};

export const tool$evaluationsCreateDatasetItem: ToolDefinition<typeof args> = {
  name: 'evaluations-create-dataset-item',
  description: `Create Dataset Item`,
  annotations: {
    title: '',
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    readOnlyHint: false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsCreateDatasetItem(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
