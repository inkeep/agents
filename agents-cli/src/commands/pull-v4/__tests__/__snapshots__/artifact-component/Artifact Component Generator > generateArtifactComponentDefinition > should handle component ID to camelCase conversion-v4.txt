import { artifactComponent } from '@inkeep/agents-sdk';
import { z } from 'zod';

export const documentTemplate = artifactComponent({
  id: 'document-template',
  name: 'Template',
  description: 'Document template component',
  props: z.object({
      title: z.string(),
    }),
});
