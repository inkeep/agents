import { externalAgent } from '@inkeep/agents-sdk';
import { partialCred } from '../credentials/partial-cred';

export const partialCredAgent = externalAgent({
  id: 'partial-cred-agent',
  name: 'Partial Cred Agent',
  description: 'Agent with partial credential reference',
  baseUrl: 'https://api.example.com/partial',
  credentialReference: partialCred,
});
