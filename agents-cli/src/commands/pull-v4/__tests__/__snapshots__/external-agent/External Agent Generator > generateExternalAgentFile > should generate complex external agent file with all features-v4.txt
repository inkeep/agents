import { externalAgent } from '@inkeep/agents-sdk';
import { weatherApiCredentials } from '../credentials/weather-api-credentials';

export const complexAgent = externalAgent({
  id: 'complex-agent',
  name: 'Complex External Agent',
  description: 'A complex external agent with credential references and advanced configuration',
  baseUrl: 'https://external-system.example.com/agents/complex',
  credentialReference: weatherApiCredentials,
});
