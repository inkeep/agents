import { Trigger } from '@inkeep/agents-sdk';

export const mixedTrigger = new Trigger({
  id: 'mixed-nullable',
  name: 'Mixed Trigger',
  enabled: true,
  messageTemplate: 'Event received: {{body.type}}',
});
