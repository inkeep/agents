import { registerEnvironmentSettings } from '@inkeep/agents-sdk';

export const minimal = registerEnvironmentSettings({
  credentials: {
    api_key: {
      id: 'api-key',
      name: '',
      type: 'memory',
      credentialStoreId: 'memory-default',
      retrievalParams: {
        key: 'API_KEY',
      },
    },
  },
});
