import { functionTool } from '@inkeep/agents-sdk';

export const emailSenderTool = functionTool({
  inputSchema: {
    type: 'object',
    properties: {},
  },
  name: 'email-sender',
  description: 'Send emails',
  execute: () => ({ sent: true }),
});
