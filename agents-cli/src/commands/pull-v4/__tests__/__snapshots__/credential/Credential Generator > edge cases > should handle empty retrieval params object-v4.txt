import { credential } from '@inkeep/agents-sdk';

export const emptyParams = credential({
  id: 'empty-params',
  name: 'Empty Params Credential',
  type: 'memory',
  credentialStoreId: 'memory-default',
  retrievalParams: {},
});
