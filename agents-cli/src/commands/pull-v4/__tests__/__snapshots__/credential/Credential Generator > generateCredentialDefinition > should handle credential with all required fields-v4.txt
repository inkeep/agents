import { credential } from '@inkeep/agents-sdk';

export const myCredential = credential({
  id: 'my-credential',
  name: 'My Credential',
  type: 'memory',
  credentialStoreId: 'memory-default',
});
