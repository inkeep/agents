import { credential } from '@inkeep/agents-sdk';

export const databaseConnectionUrlCredential = credential({
  id: 'database-connection-url',
  name: 'Database Connection URL',
  type: 'memory',
  credentialStoreId: 'env-default',
});
