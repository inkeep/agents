import { credential } from '@inkeep/agents-sdk';

export const databaseUrlCredential = credential({
  id: 'database-url',
  name: 'Database URL',
  type: 'memory',
  credentialStoreId: 'env-production',
  retrievalParams: {
    key: 'DATABASE_URL',
    fallback: 'postgresql://localhost:5432/app',
  },
});
