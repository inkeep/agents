import { credential } from '@inkeep/agents-sdk';

export const slackTokenCredential = credential({
  id: 'slack-token',
  name: 'Slack Token',
  type: 'keychain',
  credentialStoreId: 'keychain-main',
  retrievalParams: {
    service: 'slack-bot',
    account: 'my-workspace',
  },
});
