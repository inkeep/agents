import { credential } from '@inkeep/agents-sdk';

export const complex = credential({
  id: 'complex',
  name: 'Complex Credential',
  type: 'keychain',
  credentialStoreId: 'keychain-main',
  retrievalParams: {
    service: 'oauth-service',
    account: 'user@example.com',
    config: {
      timeout: 5000,
      retries: 3,
    },
  },
});
