import { mcpTool } from '@inkeep/agents-sdk';

export const apiTool = mcpTool({
  id: 'api-tool',
  name: 'API Tool',
  credential: {
    type: 'api_key',
    value: 'my-api-key',
  },
  serverUrl: 'https://api.example.com/mcp',
  transport: {
    type: 'streamable_http',
  },
});
