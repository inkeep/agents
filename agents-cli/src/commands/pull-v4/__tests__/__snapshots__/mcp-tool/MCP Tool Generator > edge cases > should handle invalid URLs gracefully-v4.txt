import { mcpTool } from '@inkeep/agents-sdk';

export const invalid = mcpTool({
  id: 'invalid',
  name: 'Invalid URL Tool',
  imageUrl: 'also-not-valid',
  serverUrl: 'not-a-valid-url',
  transport: {
    type: 'streamable_http',
  },
});
