import { project } from '@inkeep/agents-sdk';
import { onlyagent } from './agents/onlyAgent';

export const singleAgentProject = project({
  id: 'single-agent-project',
  name: 'Single Agent Project',
  models: {
    base: {
      model: 'gpt-4o-mini',
    },
  },
  agents: () => [onlyagent],
});
