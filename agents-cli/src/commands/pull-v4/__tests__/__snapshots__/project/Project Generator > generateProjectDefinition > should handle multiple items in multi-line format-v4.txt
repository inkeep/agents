import { project } from '@inkeep/agents-sdk';
import { primaryagent } from './agents/primaryAgent';
import { analyticsagent } from './agents/analyticsAgent';
import { reportingagent } from './agents/reportingAgent';
import { dataanalysistool } from './tools/dataAnalysisTool';
import { reportgeneratortool } from './tools/reportGeneratorTool';
import { legacysystemagent } from './external-agents/legacySystemAgent';
import { partnerapiagent } from './external-agents/partnerApiAgent';
import { userprofile } from './data-components/userProfile';
import { transactionhistory } from './data-components/transactionHistory';
import { dashboardcomponent } from './artifact-components/dashboardComponent';
import { reportcomponent } from './artifact-components/reportComponent';
import { databasecredentials } from './credentials/databaseCredentials';
import { apikeycredentials } from './credentials/apiKeyCredentials';

export const multiItemProject = project({
  id: 'multi-item-project',
  name: 'Enterprise AI Platform',
  description: 'Comprehensive enterprise AI platform with multiple specialized agents and shared resources',
  models: {
    base: {
      model: 'gpt-4o',
      providerOptions: {
        temperature: 0.7,
      },
    },
    structuredOutput: {
      model: 'gpt-4o',
      providerOptions: {
        temperature: 0.3,
      },
    },
    summarizer: {
      model: 'gpt-4o-mini',
      providerOptions: {
        temperature: 0.5,
      },
    },
  },
  stopWhen: {
    transferCountIs: 15,
    stepCountIs: 100,
  },
  agents: () => [primaryagent, analyticsagent, reportingagent],
  tools: () => [dataanalysistool, reportgeneratortool],
  externalAgents: () => [legacysystemagent, partnerapiagent],
  dataComponents: () => [userprofile, transactionhistory],
  artifactComponents: () => [dashboardcomponent, reportcomponent],
  credentialReferences: () => [databasecredentials, apikeycredentials],
});
