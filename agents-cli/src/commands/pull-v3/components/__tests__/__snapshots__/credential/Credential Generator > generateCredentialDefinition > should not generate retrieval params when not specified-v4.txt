import { credential } from '@inkeep/agents-sdk';

export const openaiApiKey = credential({
  id: 'openai-api-key',
  name: 'OpenAI API Key',
  type: 'memory',
  credentialStoreId: 'memory-default'
});