import { credential } from '@inkeep/agents-sdk';

export const mixed = credential({
  id: 'mixed',
  name: 'Mixed Params Credential',
  type: 'env',
  credentialStoreId: 'env-default',
  retrievalParams: {
    key: 'API_KEY',
    port: 3000,
    enabled: true,
    timeout: 30.5
  }
});
