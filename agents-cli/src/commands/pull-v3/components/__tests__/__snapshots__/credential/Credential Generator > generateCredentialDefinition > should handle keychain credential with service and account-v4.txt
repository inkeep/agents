import { credential } from '@inkeep/agents-sdk';

export const slackToken = credential({
  id: 'slack-token',
  name: 'Slack Token',
  type: 'keychain',
  credentialStoreId: 'keychain-main',
  description: 'Slack bot token stored in OS keychain',
  retrievalParams: {
    service: 'slack-bot',
    account: 'my-workspace',
  },
});
