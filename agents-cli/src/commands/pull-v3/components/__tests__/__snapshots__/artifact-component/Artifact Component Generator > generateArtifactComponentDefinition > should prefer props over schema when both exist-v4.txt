import { preview } from '@inkeep/agents-core';
import { artifactComponent } from '@inkeep/agents-sdk';
import { z } from 'zod';

export const test = artifactComponent({
  id: 'test',
  name: 'Test',
  description: 'Component with both props and schema',
  props: z.object({
      prop: preview(z.string()),
    })
});
