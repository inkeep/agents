import { artifactComponent } from '@inkeep/agents-sdk';
import { z } from 'zod';

export const test = artifactComponent({
  id: 'test',
  name: 'Test',
  description: 'Component with content type',
  props: z.object({
      content: z.string(),
    }),
  contentType: 'text/html'
});