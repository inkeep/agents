import { dataComponent } from '@inkeep/agents-sdk';
import { z } from 'zod';

export const invalidRender = dataComponent({
  id: 'invalid-render',
  name: 'Invalid Render',
  description: 'Component with invalid render',
  props: z.object({ "data": z.string().optional() }),
});
