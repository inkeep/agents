import { dataComponent } from '@inkeep/agents-sdk';
import { z } from 'zod';

export const complexData = dataComponent({
  id: 'complex-data',
  name: 'Complex Data',
  description: 'A data component with nested objects and arrays',
  props: z.object({ "user": z.object({ "id": z.string().optional(), "name": z.string().optional() }).optional(), "items": z.array(z.string()).optional() }),
});
