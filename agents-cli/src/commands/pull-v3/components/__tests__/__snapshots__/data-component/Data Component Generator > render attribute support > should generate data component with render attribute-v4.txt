import { dataComponent } from '@inkeep/agents-sdk';
import { z } from 'zod';

export const userProfileCard = dataComponent({
  id: 'user-profile-card',
  name: 'User Profile Card',
  description: 'Display user profile information',
  props: z.object({ "name": z.string(), "email": z.string(), "role": z.string().optional() }),
  render: {
    component: 'function UserProfile({ name, email, role }) { return <div className="profile"><h2>{name}</h2><p>{email}</p><p>Role: {role}</p></div>; }',
    mockData: {
      name: 'John Doe',
      email: 'john@example.com',
      role: 'Developer'
    }
  }
});
