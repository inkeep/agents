import { dataComponent } from '@inkeep/agents-sdk';
import { z } from 'zod';

export const test = dataComponent({
  id: 'test',
  name: 'Test',
  description: 'Test component with both props and schema',
  props: z.object({ "prop": z.string().optional() }),
});
