import { subAgent } from '@inkeep/agents-sdk';
import { stringTool } from '../../tools/stringTool';

export const mixedTypesSubAgent = subAgent({
  id: 'mixed-types-sub-agent',
  description: 'Agent with mixed reference types',
  prompt: 'I work with mixed types.',
  name: 'Mixed Types Agent',
  canUse: () => [stringTool],
  dataComponents: () => [stringComponent],
});
