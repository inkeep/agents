import { subAgent } from '@inkeep/agents-sdk';
import { onlyTool } from '../../tools/onlyTool';

export const singleItemAgent = subAgent({
  id: 'single-item-agent',
  description: 'Agent with single items',
  prompt: 'I am a single item agent.',
  name: 'Single Item Agent',
  canUse: () => [onlyTool],
  dataComponents: () => [onlyComponent],
});
