import { functionTool } from '@inkeep/agents-sdk';

export const simple = functionTool({
  name: 'simple-tool',
  inputSchema: {
    type: 'object',
    properties: {}
  },
  execute: async ({}) => {
      return { message: "Hello World" };
    }
});