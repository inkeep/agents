import { statusComponent } from '@inkeep/agents-sdk';
import { z } from 'zod';

export const test = statusComponent({
  type: 'test_status',
  description: 'Test status',
  detailsSchema: z.object({ "details": z.string().describe("Details field").optional() })
});
