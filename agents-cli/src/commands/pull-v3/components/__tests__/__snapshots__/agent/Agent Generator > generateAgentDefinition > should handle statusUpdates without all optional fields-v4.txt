import { agent } from '@inkeep/agents-sdk';
import { subAgent1 } from './sub-agents/subAgent1';
import { defaultSubAgent } from './sub-agents/defaultSubAgent';
import { summary } from '../status-components/summary';

export const partialStatusAgent = agent({
  id: 'partial-status-agent',
  name: 'Partial Status Agent',
  defaultSubAgent: defaultSubAgent,
  subAgents: () => [subAgent1],
  statusUpdates: {
    numEvents: 5,
    statusComponents: [summary.config]
  }
});
