# Copy to .env and fill in values:
#   cp .env.example .env

# Required — Claude API key
ANTHROPIC_API_KEY=sk-ant-...

# Optional — only needed if pushing or creating PRs from inside the container
# GITHUB_TOKEN=ghp_...
