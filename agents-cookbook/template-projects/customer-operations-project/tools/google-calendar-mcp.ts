import { mcpTool } from '@inkeep/agents-sdk';

export const googleCalendarMcp = mcpTool({
  id: 'google-calendar-mcp',
  name: 'Google Calendar',
  serverUrl: 'https://google-calendar-mcp-three.preview.inkeep.com/mcp',
});