/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: e0cfc1b48590
 */

import { oAuthSlackOauthRedirect } from "../../funcs/oAuthSlackOauthRedirect.js";
import { SlackOauthRedirectRequest$zodSchema } from "../../models/slackoauthredirectop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SlackOauthRedirectRequest$zodSchema.optional(),
};

export const tool$oAuthSlackOauthRedirect: ToolDefinition<typeof args> = {
  name: "O-auth-slack-oauth-redirect",
  description: `Slack OAuth Callback

Handles the OAuth callback from Slack after workspace installation`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oAuthSlackOauthRedirect(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
