/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 140799a80354
 */

import { workAppsSlackListWorkspaces } from "../../funcs/workAppsSlackListWorkspaces.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$workAppsSlackListWorkspaces: ToolDefinition = {
  name: "work-apps-slack-list-workspaces",
  description: `List Workspaces

List all installed Slack workspaces for the tenant`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await workAppsSlackListWorkspaces(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
