/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: e44282d12731
 */

import { subAgentsDeleteSubagentToolRelation } from "../../funcs/subAgentsDeleteSubagentToolRelation.js";
import { DeleteSubagentToolRelationRequest$zodSchema } from "../../models/deletesubagenttoolrelationop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteSubagentToolRelationRequest$zodSchema,
};

export const tool$subAgentsDeleteSubagentToolRelation: ToolDefinition<
  typeof args
> = {
  name: "sub-agents-delete-subagent-tool-relation",
  description: `Delete SubAgent Tool Relation`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await subAgentsDeleteSubagentToolRelation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
