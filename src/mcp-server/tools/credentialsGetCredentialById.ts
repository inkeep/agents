/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 8be2f6290ebd
 */

import { credentialsGetCredentialById } from "../../funcs/credentialsGetCredentialById.js";
import { GetCredentialByIdRequest$zodSchema } from "../../models/getcredentialbyidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetCredentialByIdRequest$zodSchema,
};

export const tool$credentialsGetCredentialById: ToolDefinition<typeof args> = {
  name: "credentials-get-credential-by-id",
  description: `Get Credential`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await credentialsGetCredentialById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
