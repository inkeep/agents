/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: cebd934e12b7
 */

import { artifactComponentsListArtifactComponents } from "../../funcs/artifactComponentsListArtifactComponents.js";
import { ListArtifactComponentsRequest$zodSchema } from "../../models/listartifactcomponentsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListArtifactComponentsRequest$zodSchema,
};

export const tool$artifactComponentsListArtifactComponents: ToolDefinition<
  typeof args
> = {
  name: "artifact-components-list-artifact-components",
  description: `List Artifact Components`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await artifactComponentsListArtifactComponents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
