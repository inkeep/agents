/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 4cc56086eab4
 */

import { workAppsSlackSetChannelSettings } from "../../funcs/workAppsSlackSetChannelSettings.js";
import { SlackSetChannelSettingsRequest$zodSchema } from "../../models/slacksetchannelsettingsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SlackSetChannelSettingsRequest$zodSchema,
};

export const tool$workAppsSlackSetChannelSettings: ToolDefinition<typeof args> =
  {
    name: "work-apps-slack-set-channel-settings",
    description: `Set Channel Default Agent

Set or update the default agent for a specific channel`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await workAppsSlackSetChannelSettings(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
