/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ba5cd151cd99
 */

import { refsResolveRef } from "../../funcs/refsResolveRef.js";
import { ResolveRefRequest$zodSchema } from "../../models/resolverefop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ResolveRefRequest$zodSchema,
};

export const tool$refsResolveRef: ToolDefinition<typeof args> = {
  name: "refs-resolve-ref",
  description: `Resolve Ref

Resolve a ref string (branch name, tag name, or commit hash) to its full resolved ref with type and commit hash. Pass the ref as a query parameter.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await refsResolveRef(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
