/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: cfaf0102345b
 */

import { workAppsSlackUserDisconnect } from "../../funcs/workAppsSlackUserDisconnect.js";
import { SlackUserDisconnectRequest$zodSchema } from "../../models/slackuserdisconnectop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SlackUserDisconnectRequest$zodSchema.optional(),
};

export const tool$workAppsSlackUserDisconnect: ToolDefinition<typeof args> = {
  name: "work-apps-slack-user-disconnect",
  description: `Disconnect User

Unlink a Slack user from their Inkeep account.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workAppsSlackUserDisconnect(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
