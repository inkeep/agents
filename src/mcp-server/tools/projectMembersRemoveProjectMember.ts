/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 1addae52e34f
 */

import { projectMembersRemoveProjectMember } from "../../funcs/projectMembersRemoveProjectMember.js";
import { RemoveProjectMemberRequest$zodSchema } from "../../models/removeprojectmemberop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RemoveProjectMemberRequest$zodSchema,
};

export const tool$projectMembersRemoveProjectMember: ToolDefinition<
  typeof args
> = {
  name: "project-members-remove-project-member",
  description: `Remove Project Member

Remove a user from a project. Pass role as query param to specify which role to revoke.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await projectMembersRemoveProjectMember(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
