/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ccddcb674a79
 */

import { appsUpdateApp } from "../../funcs/appsUpdateApp.js";
import { UpdateAppRequest$zodSchema } from "../../models/updateappop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateAppRequest$zodSchema,
};

export const tool$appsUpdateApp: ToolDefinition<typeof args> = {
  name: "apps-update-app",
  description: `Update App

Update an app credential configuration`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await appsUpdateApp(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
