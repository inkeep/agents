/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 7c4fa4d19cc1
 */

import { credentialsListCredentials } from "../../funcs/credentialsListCredentials.js";
import { ListCredentialsRequest$zodSchema } from "../../models/listcredentialsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListCredentialsRequest$zodSchema,
};

export const tool$credentialsListCredentials: ToolDefinition<typeof args> = {
  name: "credentials-list-credentials",
  description: `List Credentials`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await credentialsListCredentials(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
