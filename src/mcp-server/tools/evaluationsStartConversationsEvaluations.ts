/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 207acf831a71
 */

import { evaluationsStartConversationsEvaluations } from "../../funcs/evaluationsStartConversationsEvaluations.js";
import { StartConversationsEvaluationsRequest$zodSchema } from "../../models/startconversationsevaluationsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: StartConversationsEvaluationsRequest$zodSchema,
};

export const tool$evaluationsStartConversationsEvaluations: ToolDefinition<
  typeof args
> = {
  name: "evaluations-start-conversations-evaluations",
  description: `Trigger evaluations on conversations with specified evaluators`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await evaluationsStartConversationsEvaluations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
