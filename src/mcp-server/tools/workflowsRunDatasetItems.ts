/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 32d002f4f264
 */

import { workflowsRunDatasetItems } from "../../funcs/workflowsRunDatasetItems.js";
import { RunDatasetItemsRequest$zodSchema } from "../../models/rundatasetitemsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RunDatasetItemsRequest$zodSchema,
};

export const tool$workflowsRunDatasetItems: ToolDefinition<typeof args> = {
  name: "workflows-run-dataset-items",
  description: `Run dataset items

Runs dataset items for processing through the chat API`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workflowsRunDatasetItems(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
