/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: b13bbc4e90c7
 */

import { cliGetApiCliMe } from "../../funcs/cliGetApiCliMe.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$cliGetApiCliMe: ToolDefinition = {
  name: "cli-get-api-cli-me",
  description: `Get CLI user info

Get the current authenticated user and their organization for CLI usage`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await cliGetApiCliMe(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
