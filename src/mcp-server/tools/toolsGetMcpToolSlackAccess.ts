/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 46a4b8d32952
 */

import { toolsGetMcpToolSlackAccess } from "../../funcs/toolsGetMcpToolSlackAccess.js";
import { GetMcpToolSlackAccessRequest$zodSchema } from "../../models/getmcptoolslackaccessop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetMcpToolSlackAccessRequest$zodSchema,
};

export const tool$toolsGetMcpToolSlackAccess: ToolDefinition<typeof args> = {
  name: "tools-get-mcp-tool-slack-access",
  description: `Get MCP tool Slack channel access

Returns the current Slack channel access configuration for an MCP tool. If channelAccessMode is "all", the tool can post to any channel. If channelAccessMode is "selected", the tool is scoped to specific channels.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await toolsGetMcpToolSlackAccess(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
