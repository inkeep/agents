/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 15e2917281ba
 */

import { workAppsSlackDeleteWorkspace } from "../../funcs/workAppsSlackDeleteWorkspace.js";
import { SlackDeleteWorkspaceRequest$zodSchema } from "../../models/slackdeleteworkspaceop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SlackDeleteWorkspaceRequest$zodSchema,
};

export const tool$workAppsSlackDeleteWorkspace: ToolDefinition<typeof args> = {
  name: "work-apps-slack-delete-workspace",
  description: `Uninstall Workspace

Uninstall Slack app from workspace. Accepts either teamId or connectionId.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workAppsSlackDeleteWorkspace(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
