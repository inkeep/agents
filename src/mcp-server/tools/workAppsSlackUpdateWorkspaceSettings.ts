/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0208184ebab0
 */

import { workAppsSlackUpdateWorkspaceSettings } from "../../funcs/workAppsSlackUpdateWorkspaceSettings.js";
import { SlackUpdateWorkspaceSettingsRequest$zodSchema } from "../../models/slackupdateworkspacesettingsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SlackUpdateWorkspaceSettingsRequest$zodSchema,
};

export const tool$workAppsSlackUpdateWorkspaceSettings: ToolDefinition<
  typeof args
> = {
  name: "work-apps-slack-update-workspace-settings",
  description: `Update Workspace Settings

Update workspace settings including default agent`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workAppsSlackUpdateWorkspaceSettings(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
