---
"@inkeep/agents-manage-ui": patch
---

Fix model configuration JSON template to use `maxOutputTokens` instead of `maxTokens` so the placeholder example and "Template" button insert keys the runtime actually honors
