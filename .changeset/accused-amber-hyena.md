---
"@inkeep/agents-api": patch
---

Add legacy /run/auth/pow/challenge endpoint backed by a Sentinel PoW v1 security group, with dual-path verification at anonymous-session, so embedded widgets predating the pow-to-sentinel rename keep working
