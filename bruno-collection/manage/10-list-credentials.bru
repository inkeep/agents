meta {
  name: 10. List Credentials
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/manage/tenants/{{tenantId}}/projects/{{projectId}}/credentials
  body: none
  auth: bearer
}

auth:bearer {
  token: {{sessionToken}}
}

docs {
  # List Credentials
  
  Returns all credential references for a project.
  
  Credentials are stored securely and used by tools/agents.
}
