name: Claude PR Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  pr-review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0  # Full history for merge-base

      - name: Get PR Diff
        id: diff
        run: |
          # Get the diff for the PR
          DIFF=$(git diff origin/${{ github.event.pull_request.base.ref }}...HEAD)
          DIFF_SIZE=${#DIFF}

          # Get changed files list
          FILES=$(git diff --name-only origin/${{ github.event.pull_request.base.ref }}...HEAD)
          echo "changed_files<<EOF" >> $GITHUB_OUTPUT
          echo "$FILES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

          # Cap diff at 500KB to stay safely under prompt limits
          MAX_SIZE=512000
          if [ $DIFF_SIZE -gt $MAX_SIZE ]; then
            echo "diff_content<<EOF" >> $GITHUB_OUTPUT
            echo "${DIFF:0:$MAX_SIZE}" >> $GITHUB_OUTPUT
            echo "" >> $GITHUB_OUTPUT
            echo "[TRUNCATED - diff exceeded ${MAX_SIZE} bytes (was ${DIFF_SIZE}). Use 'gh pr diff ${{ github.event.pull_request.number }}' for full diff]" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
            echo "truncated=true" >> $GITHUB_OUTPUT
          else
            echo "diff_content<<EOF" >> $GITHUB_OUTPUT
            echo "$DIFF" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
            echo "truncated=false" >> $GITHUB_OUTPUT
          fi

      - name: Get Existing Review Threads
        id: threads
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          THREADS=$(gh api graphql -f query='
            query($owner: String!, $repo: String!, $pr: Int!) {
              repository(owner: $owner, name: $repo) {
                pullRequest(number: $pr) {
                  reviewThreads(first: 100) {
                    nodes {
                      isResolved
                      isOutdated
                      path
                      line
                      comments(first: 1) {
                        nodes {
                          author { login }
                          body
                        }
                      }
                    }
                  }
                }
              }
            }
          ' -f owner="${{ github.repository_owner }}" -f repo="${{ github.event.repository.name }}" -F pr=${{ github.event.pull_request.number }} --jq '.data.repository.pullRequest.reviewThreads.nodes // []')
          
          echo "review_threads<<EOF" >> $GITHUB_OUTPUT
          echo "$THREADS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Run PR Review
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          track_progress: true
          include_fix_links: true
          use_sticky_comment: true
          claude_args: |
            --agent pr-review
            --allowedTools "Task,Read,Grep,Glob,mcp__github_inline_comment__create_inline_comment,Bash(git diff:*),Bash(git log:*),Bash(git show:*),Bash(git merge-base:*),Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*)"
          prompt: |
            Review this pull request.

            **PR #${{ github.event.pull_request.number }}:** ${{ github.event.pull_request.title }}
            **Base:** ${{ github.event.pull_request.base.ref }}
            **Author:** ${{ github.event.pull_request.user.login }}

            ## Description
            ${{ github.event.pull_request.body }}

            ## Changed Files
            ```
            ${{ steps.diff.outputs.changed_files }}
            ```

            ## Diff
            ```diff
            ${{ steps.diff.outputs.diff_content }}
            ```

            ${{ steps.diff.outputs.truncated == 'true' && '> **Note:** Diff was truncated. Use `gh pr diff` if you need the full diff for judgment.' || '' }}

            ## Existing Inline Comments (for deduplication)
            Use this to avoid posting duplicate inline comments. Each object has: `isResolved`, `isOutdated`, `path`, `line`, and first comment `body`/`author`.
            ```json
            ${{ steps.threads.outputs.review_threads }}
            ```

            Skip Phase 1 (Analyze Diff) - context is provided above.
            Proceed directly to Phase 2 (Select Reviewers), then dispatch reviewers and aggregate findings.
