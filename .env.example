# ============================================
# Inkeep Agents Environment Configuration
# ============================================
# This is the single source of truth for all environment variables in the monorepo.
# Following the Cal.com pattern, all packages reference this root configuration.
#
# Setup Instructions:
# 1. Copy this file to .env and update values
# 2. For repo-specific overrides, create .env.local
# 3. For user-global settings across all repos, use ~/.inkeep/config
#
# Loading Priority (highest to lowest):
# 1. .env.local (repo-specific overrides)
# 2. ~/.inkeep/config (user global settings)
# 3. .env (main configuration)
# 4. .env.example (defaults shown here)

# ============ CORE ENVIRONMENT ============
# Environment mode: development | production | test
ENVIRONMENT=development
NODE_ENV=development
LOG_LEVEL=info

# ============ DATABASE ============
# SQLite (default for local development)
# Use the absolute path to db e.g `/Users/user/agents/local.db`. 
DB_FILE_NAME=

# PostgreSQL (for production-like setup)
# Uncomment and configure for PostgreSQL:
# DB_FILE_NAME=postgresql://user:password@localhost:5432/inkeep
# DATABASE_URL=postgresql://user:password@localhost:5432/inkeep
# DATABASE_DIRECT_URL=postgresql://user:password@localhost:5432/inkeep

# ============ API ENDPOINTS ============
# Management API (CRUD operations, configuration)
INKEEP_AGENTS_MANAGE_API_URL=http://localhost:3002
NEXT_PUBLIC_INKEEP_AGENTS_MANAGE_API_URL=http://localhost:3002

# Run API (chat completions, agent execution)
INKEEP_AGENTS_RUN_API_URL=http://localhost:3003
NEXT_PUBLIC_INKEEP_AGENTS_RUN_API_URL=http://localhost:3003

# ============ TENANT CONFIGURATION ============
# Default tenant ID for development
NEXT_PUBLIC_TENANT_ID=inkeep

# ============ AI PROVIDERS ============
# Required for agent execution
# Get your API keys from:
# Anthropic: https://console.anthropic.com/
# OpenAI: https://platform.openai.com/
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional: Inkeep API for enhanced features
INKEEP_API_KEY=

# ============ AUTHENTICATION ============
# NextAuth configuration
NEXT_PUBLIC_INKEEP_AUTH_TOKEN=

# OAuth configuration for MCP tools
DEFAULT_OAUTH_CLIENT_ID=mcp-client
OAUTH_REDIRECT_BASE_URL=http://localhost:3002
OAUTH_CLIENT_NAME=Inkeep Agent Framework
OAUTH_CLIENT_URI=https://inkeep.com
OAUTH_CLIENT_LOGO_URI=https://inkeep.com/images/logos/inkeep-logo-blue.svg

# ============ THIRD PARTY SERVICES ============
# Nango (for OAuth integrations)
NANGO_SECRET_KEY=
NANGO_HOST=http://localhost:3050
NEXT_PUBLIC_NANGO_HOST=http://localhost:3050
NEXT_PUBLIC_NANGO_CONNECT_BASE_URL=http://localhost:3051

# SigNoz (for observability)
SIGNOZ_URL=http://localhost:3080
SIGNOZ_API_KEY=

# Slack MCP Server (optional)
SLACK_MCP_URL=

# ============ DOCUMENTATION ============
# Base URL for documentation site
NEXT_PUBLIC_DOCS_BASE_URL=

# ============ NOTES ============
# For production deployments:
# - Use environment-specific values
# - Never commit sensitive keys to version control
# - Use secret management services (AWS Secrets Manager, etc.)
#
# For local development with multiple repo copies:
# - Create ~/.inkeep/config for shared settings (API keys)
# - Use .env.local for repo-specific overrides (database URLs)