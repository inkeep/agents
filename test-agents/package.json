{
  "name": "test-agents",
  "version": "0.1.11",
  "type": "module",
  "private": true,
  "dependencies": {
    "@inkeep/agents-sdk": "workspace:*",
    "@inkeep/agents-cli": "workspace:*"
  },
  "scripts": {
    "knip": "knip --directory .. --workspace test-agents --config test-agents/knip.config.ts --dependencies",
    "lint": "biome lint --error-on-warnings",
    "lint:fix": "biome check --write"
  }
}
