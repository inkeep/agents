---
title: Inkeep API
description: Explore the Inkeep Agents API endpoints for managing, running, and evaluating agents.
icon: LuSparkles
---

import { source } from '@/lib/source';

<Cards>
  {source
    .getPages()
    .filter((item) => item.url.startsWith('/api-reference/'))
    .map((item) => (
      <Card
        key={item.url}
        title={item.data.title}
        href={item.url}
        icon={item.data.icon}
      >
        {item.data.description}
      </Card>
    ))}
</Cards>

---
